# v0.8.61 Issue Coverage Matrix

> Generated from the ultracode triage workflow (12 read-only Plan agents over all 84 open
> v0.8.61 issues). Every issue has a disposition and a concrete plan. This is the input to
> `docs/V0_8_61_EXECUTION_PLAN.md`, which clusters the `design` items into agent workstreams.

Dispositions: **already-done** (branch implements it; close with the cited evidence) ·
**quick-fix** (small, safe, landable now) · **design** (real work, needs a plan/epic) ·
**defer** (too large or wrong blast radius for 0.8.61).

## Summary

- **design**: 52
- **already-done**: 16
- **defer**: 13
- **quick-fix**: 3

## already-done

### #2574 — Provider fallback chain (auto-switch on API failure). The issue body calls it 'dormant', but this branch has the ACTIVE runtime fallback: config parsing, ProviderChain advance/reset, automatic advance on recoverable errors, /provider fallback reset, and footer visibility.

_files: crates/config/src/lib.rs, crates/tui/src/tui/app.rs, crates/tui/src/tui/ui.rs, crates/tui/src/commands/groups/core/provider.rs · confidence: high_

Evidence on codex/v0.8.61: config parses fallback_providers (crates/config/src/lib.rs:636) and ProviderChain has new/position/advance/reset/is_fallback_active (lib.rs:944-1002). App holds provider_chain + last_fallback_reason (crates/tui/src/tui/app.rs:1399-1401, built at :2037-2039) and App::advance_fallback switches api_provider and records the reason (app.rs:5273-5290). The turn-error handler AUTOMATICALLY advances the chain on recoverable Network|RateLimit|Timeout errors and emits a 'Switched to X (fallback n/m)' status (crates/tui/src/tui/ui.rs:5016-5031) — and deliberately does NOT auto-switch on Authentication/InvalidInput, matching the owner's 'do not silently switch on 401' contract. Manual status + reset: crates/tui/src/commands/groups/core/provider.rs:77-122. CHANGELOG documents it as shipped: 'Active provider fallback chain (#2773)' (crates/tui/CHANGELOG.md:153-156). Recommend closing as resolved by #2773 (the deeper capability-aware routing the owner mused about can be a separate follow-up, but the issue's stated ask — auto-switch on failure with visible indication and reset — is met).

### #3204 — Route-effective context-window metadata + preflight over-limit + stop retrying context_length_exceeded. Implemented on this branch: per-route budget, pre-send preflight compaction, bounded recovery, and a single actionable failure message.

_files: crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/core/engine/context.rs, crates/tui/src/config.rs, crates/tui/src/models.rs, crates/tui/src/core/engine.rs · confidence: high_

Evidence on codex/v0.8.61: context limits are route-effective via context_input_budget_for_provider(api_provider, model) (crates/tui/src/core/engine/context.rs:582), with the Codex route capped at OPENAI_CODEX_EFFECTIVE_CONTEXT_WINDOW_TOKENS = 400_000 (crates/tui/src/config.rs:159, applied in provider_capability at :437) while public gpt-5.5 metadata stays 1.05M (crates/tui/src/models.rs:250-254). Preflight: before each dispatch the turn loop estimates input and, if over the route budget, runs emergency compaction instead of sending (crates/tui/src/core/engine/turn_loop.rs:208-233 'preflight token budget'). No-retry-of-identical-payload: on a provider context_length_exceeded rejection it runs bounded recovery then stops and returns Failed with a single 'run /compact or /clear' message rather than re-looping (turn_loop.rs:419-436; MAX_CONTEXT_RECOVERY_ATTEMPTS=2 at context.rs:62; is_context_length_error_message at context.rs:603 classifying via error_taxonomy InvalidInput). recover_context_overflow itself targets the route budget (engine.rs:2183-2188). Status line uses the effective routed model (CHANGELOG.md:1149) and prompts receive context_window_override = provider_capability(...).context_window (engine.rs:723, 2527; ui.rs:5660). Tests: compose_prompt_for_openai_api_gpt_55_uses_verified_context_window (prompts.rs:1576) and config.rs:11458 cover the alias-cap-differs case. All five acceptance bullets are satisfied; recommend closing (optionally add one explicit OpenaiCodex 400k-vs-displayed test if not already covered).

### #1791 — Synchronous tools (file_search, grep_files, list_dir) blocking turn_loop cancellation is fully fixed on this branch. The issue's proposed long-term fix (expose cancel_token through ToolContext, check it between iterations, wrap in spawn_blocking + timeout) is exactly what is implemented for all three tools.

_files: crates/tui/src/tools/spec.rs, crates/tui/src/tools/search.rs, crates/tui/src/tools/file_search.rs, crates/tui/src/tools/file.rs · confidence: high_

Already implemented. ToolContext carries the cancellation token: pub cancel_token: Option<CancellationToken> at crates/tui/src/tools/spec.rs:143 with builder with_cancel_token at spec.rs:319. file_search: run_blocking_file_search wraps spawn_blocking with timeout + token and check_cancelled is called per walker batch (crates/tui/src/tools/file_search.rs:100,118,233,250,295). grep_files: run_blocking_grep with token+timeout and check_cancelled(cancel_token) is called per-file (crates/tui/src/tools/search.rs:193) and per-line (search.rs:215), plus during collect_files (search.rs:184). list_dir: list_dir_entries_async -> run_blocking_list_dir with token+timeout and check_list_dir_cancelled per entry (crates/tui/src/tools/file.rs:899,912,933-938,966,982). Regression tests exist: test_file_search_respects_cancel_token (file_search.rs:505) and list_dir tests (file.rs:1870). The two linked PRs (#1790 file_search, #1839 grep_files) and the list_dir path have all landed. No action needed; close as fixed.

### #719 — Teach parent that subagent results are self-reports needing re-verification. The exact 'Trust model' block the issue specified is already in the agent_spawn description verbatim, plus reinforcing language in the agent_open API surface.

_files: crates/tui/src/tools/subagent/mod.rs, crates/tui/src/tools/subagent/tests.rs, crates/tui/src/tools/subagent/mailbox.rs · confidence: high_

DONE on this branch. Evidence: crates/tui/src/tools/subagent/mod.rs:3234-3238 embeds the verbatim '## Trust model: subagent results are self-reports, not verified facts' header plus the exact side-effect re-verify table (URL->fetch_url, file->read_file, edit->read_file, HTTP->status, git->git_status, test->run_tests, process->exec_shell) requested in the issue body. Reinforced at mod.rs:2964 ('Sub-agent results are self-reports. Re-verify claimed side effects...') and the agent_open projection: default_agent_run_verification returns status 'self_report_only' with summary 'treat the result summary as a child self-report' (mod.rs:870-876), and the receipt artifact says 'verify before treating as fact' (mod.rs:932-938). Test coverage: tests.rs:508 asserts description.contains('Sub-agent results are self-reports'). NOTE the issue's step-2 'paired deletion' (collapse the 7-way concat! prompt duplication, net -40 LOC) was NOT done and is not required for the behavioral fix; the owner's 2026-06-10 comment scoped this issue to 'a small prompt addition' which has landed. Recommend closing the trust-language ask; spin the prompt-dedup refactor into the #2958 prompt-audit work if still desired.

### #1806 — Sub-agent hardcoded 120s API timeout. The issue's stated minimum fix (configurable timeout via [subagents]) plus the checkpoint/resume follow-up (#2029) and full-registry write-tool inheritance are all implemented on this branch.

_files: crates/tui/src/config.rs, crates/tui/src/tools/subagent/mod.rs, crates/tui/src/core/engine.rs, crates/tui/src/tools/subagent/tests.rs · confidence: high_

DONE on this branch. Ask #1 (configurable timeout): [subagents] api_timeout_secs is parsed at crates/tui/src/config.rs:1694, resolved+clamped by subagent_api_timeout_secs() (config.rs:3179-3196) against DEFAULT=120 / MIN=1 / MAX=1800 constants (config.rs:31-39, comments cite '#1806, #1808'), wired into the runtime via Config::subagent_api_timeout -> SubAgentRuntime::with_step_api_timeout at engine.rs:1232 and 1827; the per-step API call uses runtime.step_api_timeout (mod.rs:5254). The hardcoded 120s is now only the default (DEFAULT_STEP_API_TIMEOUT, mod.rs:98). Ask #2 (write-tool access): default sub-agents inherit the FULL parent registry including write_file since v0.6.6 (mod.rs:461-467, 3287 'Default behavior is full registry inheritance'); the old read-only allowed_tools default is deprecated. Ask #3 (checkpoint/streaming): API-timeout now preserves a continuable checkpoint and agent_eval resumes from it -- SubAgentStatus::Interrupted (mod.rs:593), checkpoint fields (mod.rs:619,1466), continued_from_checkpoint (mod.rs:696), tested by api_timeout_preserves_checkpoint_and_agent_eval_continues_from_it (tests.rs:1525). Triage doc already records this as rolled into #3216/#3217 (docs/V0_8_61_RELEASE_TRIAGE.md:207). Recommend closing as fixed.

### #2652 — Clipped sub-agent eval output mistaken by the model for complete evidence. The model-facing completion sentinel now explicitly flags clipping and directs the model to fetch the full transcript; the deeper Kimi-audit 'preview + evidence_handle on every lane' framing is only partially realized.

_files: crates/tui/src/tools/subagent/mod.rs, crates/tui/src/tools/subagent/tests.rs · confidence: medium_

Core failure mode is FIXED on this branch. The parent's in-context completion sentinel now carries result_clipped, summary_complete (=!clipped), and next_action='call_agent_eval' when the child result exceeds the 240-char preview (subagent_done_sentinel at mod.rs:4874-4889; clip predicate subagent_result_clipped at mod.rs:6788-6794). This is model-facing (emitted into the transcript as <codewhale:subagent.done>), not just a UI affordance, so the model is told its summary is incomplete. Tests: tests.rs:2381-2391 (subagent_done_sentinel_flags_clipped_result) and tests.rs:2372-2374 (unclipped path). The legacy agent_result tool returns the COMPLETE result string (ToolResult::json(&result), mod.rs:3793), and agent_eval exposes a transcript_handle artifact (mod.rs:924-931) plus verification status 'self_report_only' (mod.rs:870-876). What is NOT done: the Kimi-audit comment's richer ask -- every lane result carrying a structured preview + evidence_handle/artifact_refs pair and synthesis steps re-opening handles per-claim (ties to #3142 run ledger, #3180 per-lane handles). That is design-level and out of scope for 0.8.61. Recommend closing the original clipped-vs-complete bug as fixed and tracking the structured-evidence framing under #3142/#3180.

### #3102 — First-class clarification-question requests for agents. The request_user_input primitive is fully built end-to-end (tool schema, engine handling, TUI modal) matching the issue's JSON shape; only the headless/app-server typed event and opt-in visibility remain, and the owner already retargeted those to v0.8.62.

_files: crates/tui/src/tools/user_input.rs, crates/tui/src/tui/user_input.rs, crates/tui/src/core/engine/approval.rs, crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/core/engine/handle.rs · confidence: medium_

Substantially DONE on this branch; the remaining slice is explicitly v0.8.62. Implemented: the request_user_input tool with the exact issue schema -- 1-3 questions, header/id/question, 2-3 options with label+description (crates/tui/src/tools/user_input.rs:10-164, validation at :38-84). Engine wiring: REQUEST_USER_INPUT_NAME (engine/tool_catalog.rs:22), dispatch in turn_loop.rs:1990-1994, await_user_input with USER_INPUT_TIMEOUT (engine/approval.rs:109-130), submit_user_input/cancel_user_input on the handle (engine/handle.rs:103-120), tool gated behind with_user_input_tool() (tool_setup.rs:66, command_palette.rs:102). TUI rendering: crates/tui/src/tui/user_input.rs with modal render tests (user_input_modal_calls_out_required_action_and_controls :434, user_input_modal_renders_custom_response_state :443). Guardrail to not over-ask is in the constitution (constitution.md:250, base.md:133). NOT done (and the issue was retargeted to v0.8.62 by the owner 2026-06-12): the headless/API typed-event path (grep of crates/app-server and crates/protocol for user_input returns 0 hits, so non-interactive runs have no typed event/deterministic fallback) and the opt-in/visibility behavior requested after the 'jarring modal' feedback. Recommend leaving on v0.8.62; if a 0.8.61 win is wanted, the only safe slice is confirming non-interactive runs fail deterministically rather than hang -- but that is design, not a drop-in. Confidence medium because the headless contract is genuinely unbuilt.

### #874 — Mid-turn intervention with finer granularity than 'Esc the whole turn' now exists. The issue's central premise — that decide_submit_disposition returns Queue uniformly while is_loading — is no longer true on this branch. The engine also gained an explicit pending-steer channel. Only the narrowest sub-request (a per-item 'send now' button inside the PendingInputPreview widget for messages queued during active text streaming) is unbuilt, and that path is now a rare edge case because most busy states auto-steer.

_files: crates/tui/src/tui/app.rs, crates/tui/src/tui/ui.rs, crates/tui/src/tui/widgets/pending_input_preview.rs, crates/tui/src/core/engine/turn_loop.rs · confidence: medium_

Core is implemented: (1) crates/tui/src/tui/app.rs:5028-5043 decide_submit_disposition now returns SubmitDisposition::Steer when busy-but-not-streaming-text (streaming_message_index.is_none()) — exactly the multi-todo Agent scenario in the issue where the model is waiting on tools/sub-agents — and only returns Queue while actively streaming text. (2) Manual override: crates/tui/src/tui/ui.rs:4135-4182 wires Ctrl+Enter to force steer_user_message into the current turn in any busy state (the 'promote selected queued message into an immediate steer' the issue asked for). (3) Steer plumbing: app.rs:4997-5011 push_pending_steer/drain_pending_steers, app.rs:1719/1728 pending_steers + submit_pending_steers_after_interrupt; ui.rs:7317-7358 submit_or_steer_message dispatches Immediate/Queue/Steer; ui.rs:7365 merge_pending_steers coalesces. Tests at app.rs:7589-7668 assert the Immediate/Steer/Queue matrix and steer ordering. RESIDUAL (separate, design-sized): a per-row 'send now' affordance in crates/tui/src/tui/widgets/pending_input_preview.rs (renders queued items at :47/:120 but has no per-item action) to steer one chosen queued message while text is actively streaming. Recommend closing 874 as addressed and, if still wanted, filing a small follow-up for the per-item preview action.

### #1812 — The two concrete Windows freeze mechanisms captured in this issue's thread are fixed on the branch. The exec_shell descendant-pipe hang (the detailed second finding / PR #2498) is fixed via Windows Job Objects, and the crossterm event::poll is now bounded (never INFINITE), neutralizing the original root-cause hypothesis. The only un-landed items are the optional defense-in-depth layers (a heartbeat watchdog thread and a post-exec FlushConsoleInputBuffer), which the contributor themselves framed as complementary, not required.

_files: crates/tui/src/tui/ui.rs, crates/tui/src/tui/tools/shell.rs, crates/tui/src/core/engine/turn_loop.rs · confidence: medium_

Evidence the concrete hangs are closed: (1) Bounded poll — crates/tui/src/tui/ui.rs:2859 calls event::poll(poll_timeout) where poll_timeout is derived at ui.rs:2823-2828 from active_poll_ms (24ms) / idle_poll_ms (48ms) (ui.rs:9533-9543, constants UI_ACTIVE_POLL_MS/UI_IDLE_POLL_MS at :154-155) and clamped to >=1ms at clamp_event_poll_timeout (ui.rs:9545-9548). There is no INFINITE poll anywhere, so the 'WaitForSingleObject(INFINITE) never returns' chain in the issue body cannot occur. (2) Descendant-pipe hang — crates/tui/src/tui/tools/shell.rs:29-32 imports Windows JobObjects; :259-282 creates a Job per shell child, sets JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE, assigns the child, and TerminateJobObject before joining stdout/stderr readers (PR #2498's approach, including the detached-descendant regression test the PR described). (3) Engine cancellation between tool batches also landed (commit a6946812d, #3216/#2211). NOT done (optional, design-sized, low ROI now): the standalone watchdog thread (proposed crates/tui/src/tui/tui_watchdog.rs) and FlushConsoleInputBuffer after exec_shell. Recommend closing 1812 against the fixed mechanisms; if a watchdog is still desired it should be its own scoped issue, not a 0.8.61 blocker.

### #3078 — The constitution prompt is parameterized per-model and all six acceptance criteria are effectively met: non-DeepSeek models no longer receive V4/one-million/$0.14 claims, DeepSeek keeps its full self-model, unknown models get honest fallbacks, no unresolved placeholders survive, and substitution is once-per-session (cache-stable). I ran the four targeted tests on this branch and they pass. The only residue is two incidental static prose strings the issue also asked to genericize.

_files: crates/tui/src/prompts.rs, crates/tui/src/pricing.rs, crates/tui/src/prompts/constitution.md, crates/tui/src/prompts/constitution.yaml · confidence: high_

Verified done: crates/tui/src/prompts.rs:845-902 apply_model_template substitutes {context_window_note} (models::context_window_for_model with honest 'provider-dependent and not known to the harness' fallback at :867-872), {subagent_economics} (pricing::input_cost_note at :875, generic fallback at :876-878), {model_thinking_note} (gated on models::model_supports_reasoning at :882), and {model_characteristics} (V4_MODEL_CHARACTERISTICS vs GENERIC_MODEL_CHARACTERISTICS, deepseek+v4 predicate at :892-899). pricing::input_cost_note exists at crates/tui/src/pricing.rs:229-235. Placeholders are live in crates/tui/src/prompts/constitution.md:297/402/409/413. Tests pass (ran `cargo test -p codewhale-tui --bin codewhale-tui compose_prompt_for_`): compose_prompt_for_v4_model_keeps_v4_facts, _for_kimi_uses_model_accurate_facts (asserts no V4/no one-million/no $0.14), _for_openai_api_gpt_55_uses_verified_context_window, _for_unknown_model_uses_honest_fallbacks (prompts.rs:1547-1601), plus assert_no_unresolved_model_placeholders at :1532. RESIDUAL quick-fix tail (genericize two static mentions the issue listed): constitution.md:467 'so DeepSeek prefix-cache reuse stays high' -> 'so provider prefix-cache reuse stays high'; constitution.md:469 'where Flash V4 with thinking off' -> 'where the configured fast model with thinking off'. Note the YAML still lacks these placeholders/strings (constitution.yaml has no {..} placeholders), so the documented YAML->MD sync check cannot pass until the separate constitution-retrospective reconciliation lands; editing the .md directly here matches how the placeholders were already introduced. Recommend closing 3078 as done and folding the two-string genericization into the next constitution edit.

### #3028 — Clickable TUI sidebar: click-to-act on task/agent rows plus stop targets for background tasks. The click-to-act dispatch layer is implemented and tested; only the cosmetic trailing [x] stop glyph from the issue is absent (cancel is reachable via the row's detail line instead).

_files: crates/tui/src/tui/mouse_ui.rs, crates/tui/src/tui/sidebar.rs, crates/tui/src/tui/app.rs · confidence: high_

Treat the core as already-done; the click->command dispatch the issue specifies is present. Evidence: SidebarHoverRow carries pub click_action: Option<String> (crates/tui/src/tui/app.rs:1254); sidebar_click_action walks sections containment-then-row_y and returns the row's command (crates/tui/src/tui/mouse_ui.rs:503-523); left-clicks dispatch through the command-palette pipeline via ViewEvent::CommandPaletteSelected{ExecuteCommand} (crates/tui/src/tui/mouse_ui.rs:399-404) and also feed the right-click 'Run' menu (mouse_ui.rs:708); background_task_click_actions returns (show, cancel) routing shell_* ids to /jobs and others to /task, with cancel only for running|queued (crates/tui/src/tui/sidebar.rs:812-824, wired through task_panel_rows at 856 and render_sidebar_tasks at 837); tests sidebar_click_resolves_row_actions_inside_section and sidebar_click_outside_section_resolves_to_none exist (mouse_ui.rs:1226,1298). RESIDUAL quick-fix only if desired: add a styled trailing [x] span to running background-task label lines in task_panel_rows and a stop-zone hit test on the last ~3 cells (mouse_ui.rs:503 mapping to the row's cancel action) — about 15-25 lines, plus one parity test; verify with cargo test -p codewhale-tui --bin codewhale-tui sidebar_click and cargo clippy -p codewhale-tui -- -D warnings. Confirm Enter/Space keyboard focused-row model (umbrella #2018) is intentionally out of scope.

### #963 — Word-wrap truncation of long CJK text. Reported on v0.8.20 (~/.deepseek paths). The markdown renderer has since been rewritten and now hard-breaks overlong/no-whitespace runs at character boundaries with unicode-width, with a dedicated CJK regression suite.

_files: crates/tui/src/tui/markdown_render.rs · confidence: high_

Already implemented on this branch. The renderer was rebuilt into a width-independent parse + width-dependent render (crates/tui/src/tui/markdown_render.rs:3 documents the rewrite away from the old single-pass render_markdown). wrap_text (crates/tui/src/tui/markdown_render.rs:1168) detects words wider than the line and calls push_word_breaking_chars to hard-break at char boundaries using unicode-width (markdown_render.rs:1182-1188, char_display_width at :1136). The exact #963 bug class (no-whitespace CJK runs that overflow/truncate) is pinned by a regression suite at markdown_render.rs:1759-1815: paragraph_wrap_breaks_overlong_word_at_width_40 asserts every line width <= budget AND that full content is preserved (combined.matches('a').count() == 200), and the comment at :1764-1766 explicitly names 'no-whitespace CJK runs'. Recommend closing as fixed; optionally add one CJK-literal fixture (a 300-char Han string at width 40) to make the #963 case explicit. No production code change required.

### #3058 — Complaint that a .codewhale is generated in every cwd even for Q&A. On this branch project context is ephemeral/in-memory and sessions/snapshots/logs resolve under $HOME/.codewhale; no startup code creates .codewhale in the workspace. Maintainer triage doc still lists it as unverified.

_files: crates/tui/src/project_context.rs, crates/tui/src/session_manager.rs, crates/tui/src/snapshot/paths.rs · confidence: medium_

Appears already fixed; recommend verification rather than feature work. Evidence: load_project_context generates context in memory with source_path=None and the generated text literally states 'No .codewhale/instructions.md file was written.' (crates/tui/src/project_context.rs:917-925); the test at project_context.rs:1485-1489 asserts 'loading context should not create a .codewhale directory'. Ephemeral behavior landed in commit b424848f7 'fix(context): keep generated project context ephemeral'. Session storage prefers ~/.codewhale/sessions (session_manager.rs:620 default_sessions_dir -> resolve_state_dir), snapshots use ~/.codewhale/snapshots (snapshot/paths.rs:45-50), logs use $HOME/.codewhale/logs (runtime_log.rs:213). The only workspace-local .codewhale writers are on-demand (oversized pastes -> .codewhale/pastes/, commit 9c6830577; fleet ledger; runtime-api state). No startup/Q&A path creates the dir. To close: add a regression test that runs a Q&A-style session-start over a temp workspace and asserts !workspace.join('.codewhale').exists(), extending the existing project_context.rs:1485 assertion to the full boot path. Confidence medium because docs/V0_8_61_RELEASE_TRIAGE.md:214 still calls #3058 'net-new with no shipped signal' pending a clean manual repro of a bare codewhale launch in an empty dir.

### #3066 — Cost tracking for non-DeepSeek models is implemented: expanded USD pricing table, has_pricing_for_model, and tests all present on branch.

_files: crates/tui/src/pricing.rs, docs/V0_8_61_RELEASE_TRIAGE.md · confidence: high_

Already landed (commit 1faf782a1, PR #3201). Evidence: crates/tui/src/pricing.rs:116 `pub fn has_pricing_for_model`; :152-166 curated USD-only rows for `moonshotai/kimi-k2.6`, `z-ai/glm-5.1`, `minimax/minimax-m3`, `arcee-ai/trinity-large-thinking`, `openai/gpt-5.5(+pro)`, and the qwen3.6/3.7 family via `usd_only_pricing(...)`; module doc :3-4 documents USD-only-vs-CNY split; tests :397-417 `curated_usd_only_models_have_pricing_and_accrue_cost` assert Some + has_pricing for kimi/glm/qwen3.6-plus/trinity/gpt-5.5, plus :392 unknown-model-returns-none. Matches the issue's acceptance criteria. docs/V0_8_61_RELEASE_TRIAGE.md:200,224 records PR #3201 closes it. Safe to close with credit to @mvanhorn.

### #2960 — Rebrand update path (deepseek update + npm). Fully implemented on this branch across the binary self-update, the npm deprecation, and the docs. The repo's own V0_8_61_RELEASE_TRIAGE.md already marks it close-with-credit.

_files: crates/cli/src/update.rs, npm/deepseek-tui/package.json, npm/deepseek-tui/scripts/deprecation-notice.js, docs/REBRAND.md, README.md · confidence: high_

DONE. (1) Self-update no longer dies with a spawn error: crates/cli/src/update.rs:32 calls is_legacy_binary(&current_exe) at the update entrypoint and bails with legacy_binary_message; is_legacy_binary (line 196-203) matches exe names starting with "deepseek"; legacy_binary_message (line 205-233) prints exact per-install-method migration commands for npm, cargo, Homebrew, and release binaries; tests at update.rs:699-715 cover legacy detection. (2) npm rename path: npm/deepseek-tui/package.json declares the deprecated package ("not republished", homepage -> docs/REBRAND.md) with a postinstall scripts/deprecation-notice.js that prints the migration banner. (3) Docs: README.md:92 has an "Upgrading from the legacy deepseek-tui package?" callout, and docs/REBRAND.md:12-185 has the full cargo/npm/brew migration section. Action: close #2960 (and #2917/#2924) with credit; no code change needed. Optional nit only: README.md:86 still recommends `brew install deepseek-tui` for the legacy tap, which is intentional per REBRAND.md:119-120.

### #3069 — The DEEPSEEK_BLUE -> WHALE_ACCENT_PRIMARY consumer rename is complete on this branch. Only the two intentional deprecated-alias definitions remain; all real consumers now use WHALE_ACCENT_PRIMARY.

_files: crates/tui/src/palette.rs, docs/V0_8_61_RELEASE_TRIAGE.md · confidence: high_

Already implemented by commit ecf700690 ("Rename DeepSeek blue consumers to whale accent"; the triage doc at docs/V0_8_61_RELEASE_TRIAGE.md:233 records PR #3197 as closing #3069). Evidence: only 2 non-test references to DEEPSEEK_BLUE remain and both are the deprecated aliases themselves — crates/tui/src/palette.rs:87 (pub const DEEPSEEK_BLUE_RGB = WHALE_ACCENT_PRIMARY_RGB) and crates/tui/src/palette.rs:235 (pub const DEEPSEEK_BLUE: Color = WHALE_ACCENT_PRIMARY), each carrying a #[deprecated] note (palette.rs:85,233). WHALE_ACCENT_PRIMARY now has 60 references; DEEPSEEK_SKY is correctly left intact (palette.rs:237). RGB is unchanged (alias points at the same value), so visual appearance is identical. Acceptance criteria are met; the issue can be closed (alias removal is the future cleanup the deprecation note already schedules).

## quick-fix

### #3208 — Not a bug: both artifacts are intentional. codewhale-linux-x64 is the bare CLI dispatcher binary (what the in-app updater and npm wrapper download); codewhale-linux-x64.tar.gz is the platform bundle (CLI + TUI + install.sh) for manual installs. The confusion is pure documentation: the GitHub release body and INSTALL.md never explain why both exist side by side. Fix is a few clarifying lines, no code or pipeline change.

_files: scripts/release/generate-release-body.sh, docs/INSTALL.md, .github/workflows/release.yml · confidence: high_

Root cause confirmed: release.yml builds the bare binary (artifact_name codewhale-linux-x64, .github/workflows/release.yml:116) and separately bundles the archive (release.yml:308-310 bundle linux-x64 ... tar.gz). The updater consumes the bare binary (crates/cli/src/update.rs:747 ('codewhale','linux','x86_64','codewhale-linux-x64')) and REQUIRED_RELEASE_ASSETS (crates/tui/src/tui/ui.rs:176-194) lists BOTH intentionally. EXACT change (docs only, ~15 lines, safe to land unattended): (1) crates/tui/src/prompts/../scripts/release/generate-release-body.sh — the line 'Individual binaries are also attached below for scripting and the npm wrapper.' (in the static install section) is the only mention of the bare binaries; expand it to one sentence: e.g. 'Each platform also has bare, unarchived binaries (codewhale-<platform>, codewhale-tui-<platform>) used by the npm wrapper and the in-app `codewhale update`; the `.tar.gz`/`.zip` archives are the recommended manual download and additionally bundle an install script.' (2) docs/INSTALL.md Section 6 (lines 259-291, 'Manual download from GitHub Releases') — add a short note distinguishing the bare per-platform binary from the .tar.gz bundle so the releases page is self-explanatory. Test: run `bash scripts/release/generate-release-body.sh v0.8.61 CHANGELOG.md` and eyeball the rendered Install section; `markdownlint`/CI doc checks unaffected. No Rust, no workflow logic touched.

### #3068 — Audit and document legacy .deepseek/ compatibility paths. The consolidation helper the issue asks about already exists in config; the missing deliverable is the documentation artifact (docs/LEGACY_PATHS.md) plus a keep/deprecate/remove note per path. Doc-only and safe to land unattended.

_files: docs/LEGACY_PATHS.md, crates/config/src/lib.rs, crates/tui/src/skills/mod.rs, crates/tui/src/prompts.rs · confidence: medium_

QUICK-FIX (doc-only). The consolidation already exists: config/src/lib.rs:3427-3489 defines CODEWHALE_APP_DIR/LEGACY_APP_DIR, codewhale_home(), legacy_deepseek_home(), resolve_state_dir() (read path with .codewhale->.deepseek fallback) and ensure_state_dir() (write path), with a parallel project-local resolver at lib.rs:3491+. Change: create docs/LEGACY_PATHS.md (NOT present today; confirmed by `ls docs/`) that tables each runtime .deepseek reference -> its .codewhale target -> decision (keep-as-fallback / deprecate / remove). Cover the call sites named in the issue that still hardcode the legacy path rather than routing through resolve_state_dir: crates/tui/src/skills/mod.rs:476 (~/.deepseek/skills), crates/tui/src/prompts.rs:71 (LEGACY_HANDOFF_RELATIVE_PATH = ".deepseek/handoff.md"), crates/tui/src/workspace_trust.rs:4, crates/tui/src/session_manager.rs:621, crates/tui/src/skill_state.rs:8, crates/tui/src/tools/skill.rs:103, crates/tui/src/snapshot/mod.rs:5, crates/tui/src/workspace_discovery.rs:12. Recommend keep-as-fallback for all (matches the v0.8.44 migration contract at lib.rs:3420-3425). Test: none needed (doc); verify links/paths resolve. NOTE: the issue's optional "consolidate scattered fallback into one helper" sub-goal is the only non-doc part and would route the hardcoded sites through resolve_state_dir — that is a separate small refactor and should be a follow-up, kept out of the unattended doc change.

### #3012 — Confirmed still open: global ~/.codewhale/instructions.md is NOT auto-loaded. global_context_relative_paths() returns only the 6 AGENTS.md + WHALE.md variants; instructions.md is only honored at the project level. Small, localized change in one file with an existing test pattern.

_files: crates/tui/src/project_context.rs · confidence: high_

Exact change, all in crates/tui/src/project_context.rs: (1) Add three constants next to the existing globals (after line 65): GLOBAL_INSTRUCTIONS_RELATIVE_PATH = &[".codewhale","instructions.md"], GLOBAL_INSTRUCTIONS_VENDOR_NEUTRAL_PATH = &[".agents","instructions.md"], GLOBAL_INSTRUCTIONS_LEGACY_PATH = &[".deepseek","instructions.md"]. (2) In global_context_relative_paths() (line 825) change the return array length from 6 to 9 and insert the three instructions paths AFTER the AGENTS variants and BEFORE the WHALE variants, to honor the issue's stated priority AGENTS > instructions > WHALE: [AGENTS_RELATIVE, AGENTS_VENDOR_NEUTRAL, AGENTS_LEGACY, INSTRUCTIONS_RELATIVE, INSTRUCTIONS_VENDOR_NEUTRAL, INSTRUCTIONS_LEGACY, WHALE_RELATIVE, WHALE_VENDOR_NEUTRAL, WHALE_LEGACY]. The existing load_global_agents_context() loop (line 884) already iterates the array, loads the first existing file, merges with project context via merge_global_and_project_instructions (line 857), and only the WHALE filename triggers the deprecation warning (line 890), so instructions.md needs no warning logic. Update the doc comment block at lines 56-59 and 875-882 to list the new precedence. Test: add a unit test mirroring the existing global-context tests (e.g. near project_context.rs:1099-1117) that writes ~/.codewhale/instructions.md into a temp HOME with no AGENTS.md and asserts the loaded global context contains its body; run `cargo test -p codewhale-tui project_context`. Stays well under 60 lines and 1 file (+1 test).

## design

### #414 — Subagent permission auto-derivation (child = parent perms intersect child profile allowlist). NOT implemented: children currently get the FULL parent registry; allowed_tools is a per-name visibility filter, not a capability intersection.

_files: crates/tui/src/tools/subagent/mod.rs, crates/tui/src/tui/subagent_routing.rs · confidence: high_

Current seams: SubAgentToolRegistry (crates/tui/src/tools/subagent/mod.rs:6492-6560) builds the full agent surface via with_full_agent_surface and only narrows by name via is_tool_allowed (mod.rs:6549-6560); role_can_delegate_writes (mod.rs:6520) plus auto_approve are the only capability gates. AgentWorkerToolProfile::Inherited (mod.rs:653, 2135) means no derivation. Implementation: (1) define a conservative child profile policy (read/write/shell/network capability classes); (2) compute effective = parent_capability_state INTERSECT child_profile at registry build time, denying capabilities the child profile does not allow even when the parent could; (3) route Whaleflow leaves and agent_open through the same policy; (4) record permission decisions in the worker/run trace. Per the owner's 2026-06-12 comment this is explicitly coupled to #3096/#3181 and was slipped from v0.8.8 to v0.9.0; needs an epic + tests for denied write/shell/network on a child even when the parent has them. Too large and cross-cutting to land unattended in 0.8.61.

### #1786 — Shell PID hang + premature LIVE-state exit during long tasks. Partially mitigated: session-load orphan reaping and turn_started_at clearing exist, but the foreground shell-hang root cause and full work-queue sync are not solved.

_files: crates/tui/src/task_manager.rs, crates/tui/src/tools/shell.rs, crates/tui/src/runtime_threads.rs, crates/tui/src/tui/ui.rs · confidence: high_

Already landed (cite when commenting): load_state marks any persisted Running task Failed with 'Interrupted by process restart; prior process is not attached' and fails its running tool_calls, and refuses to requeue it (crates/tui/src/task_manager.rs:1516-1547; test running_tasks_are_not_requeued_after_restart at :1816). LIVE-state animation is cleared by setting turn_started_at = None on turn end/reconcile (crates/tui/src/tui/ui.rs:1867, 4776, 4797; #2468 9ef8fe7). Foreground hangs are bounded by clamp(1000, 600_000) and SIGKILL of the whole process group (crates/tui/src/tools/shell.rs:934, 139-167). Remaining design work for a future pass: (a) a task_manager liveness/healthcheck that probes live runtime task ids (not just on load) so an in-session stalled task_shell_start cannot keep the Work list desynced from the agent; (b) reconcile the 'task done in Work list but agent still waiting' race between task_manager status and runtime_threads turn state. Seams: crates/tui/src/task_manager.rs:719-783 (no live-PID probe loop), crates/tui/src/runtime_threads.rs engine init. Maintainer (2026-05-31) said keep open; not a safe unattended single patch.

### #3015 — Land constitution system prompt + reconcile YAML<->MD, add --check/CI sync, ride-alongs, close superseded prompt issues. Partially done: constitution.md is the live prompt and the YAML+renderer exist, but renderer drift, the --check drift gate, and CI sync are still missing.

_files: crates/tui/src/prompts.rs, crates/tui/src/prompts/constitution.yaml, crates/tui/src/prompts/render_constitution.py, crates/tui/src/prompts/constitution.md, .github/workflows/ci.yml · confidence: high_

Already landed: BASE_PROMPT = include_str!("prompts/constitution.md") (crates/tui/src/prompts.rs:315); personality folded into the YAML (prompts.rs:1014); render_constitution.py + constitution.yaml present (crates/tui/src/prompts/). Still open per owner 2026-06-11 comment and confirmed here: render_constitution.py output (~283 lines) does not match committed constitution.md (517 lines), it bakes the default model id over the {model_id} placeholder, and YAML/MD tier numbers disagree; there is no --check drift mode and no CI sync step alongside scripts/check-provider-registry.py. Implementation seams: reconcile crates/tui/src/prompts/constitution.yaml + render_constitution.py to byte-match constitution.md, add a placeholder-preserving render mode plus --check (exit non-zero on mismatch), wire it into .github/workflows/ci.yml, then add the two one-line ride-alongs (#1842 Language scoping, #719 sub-agent self-report caveat) and close #2950/#2953/#2958/#2959/#3008/#3010 with section pointers. This is a multi-file documentation/tooling epic, not a single safe patch.

### #3074 — Per-theme accent/selection color override for dark themes (Signal Gold #F6C453 makes selected list rows unreadable). NOT implemented: there is no user-facing accent/selection override and themes are fixed palettes.

_files: crates/tui/src/palette.rs, crates/tui/src/settings.rs, crates/tui/src/theme_qa_audit.rs, crates/config/src/lib.rs · confidence: high_

Seams: the accent is the hardcoded WHALE_ACCENT_PRIMARY_RGB = (246,196,83) (crates/tui/src/palette.rs:30), used to build per-theme palettes; each theme has a fixed selection_bg (palette.rs:882, 925, 968, ...). Theme selection is name-only via normalize_theme_name (palette.rs:1284) and TuiPrefs exposes only theme + font_size (crates/tui/src/settings.rs:50-56) with no accent/selection field in settings or crates/config. Two viable scopes: (quick, but not trivially safe) audit the dark-theme selection_bg/SELECTION_TEXT contrast and darken/swap the gold-on-selection so selected text is legible in tokyo-night/catppuccin-mocha/dracula/dark — touches palette.rs + theme_qa_audit.rs only; OR (the issue's actual ask) add an optional accent_color/selection_color override field to TuiPrefs + config schema, thread it through palette construction, validate hex, and add theme_qa contrast tests. The override path is real config-surface design work; even the contrast-only fix needs careful per-theme review against theme_qa_audit.rs, so this is design rather than a blind unattended patch.

### #3093 — Next-wave README + website locales for Korean, Spanish, Brazilian Portuguese. NOT implemented and depends on the #3090 localization foundation; large reviewed-translation effort.

_files: web/lib/i18n/config.ts, web/lib/i18n/get-dictionary.ts, web/middleware.ts, README.md · confidence: high_

Current state: web ships only locales = ['en','zh'] (web/lib/i18n/config.ts:1) with dictionaries keyed to those (web/lib/i18n/get-dictionary.ts:3) and a [locale] route (web/app/[locale]); README set is en/ja/vi/zh only (README.md, README.ja-JP.md, README.vi.md, README.zh-CN.md). Delivering ko/es/pt-BR means: add the three README files with symmetric language headers, add locale entries + dictionary files under web/lib/i18n/dictionaries, extend Accept-Language detection (web/middleware.ts) for ko/es/pt/pt-BR, add native-name selector entries, and verify mobile/desktop overflow. The issue itself scopes acceptance to AFTER the #3090 selector/drift-check foundation lands and gates closure on #3090 drift coverage, and it is explicitly splittable into per-locale PRs requiring human translation review. Too large and human-review-dependent for an unattended 0.8.61 patch; treat as a tracked localization epic.

### #426 — Per-agent tool filtering at the ToolRegistry level (build/plan/explore/review personas see different tool lists, built once per turn) is NOT implemented as specified. A capability-filter primitive exists but is unused, and both the parent turn and sub-agents wire tools differently from what the issue asks.

_files: crates/tui/src/tools/registry.rs, crates/tui/src/tools/subagent/mod.rs, crates/tui/src/core/engine/tool_setup.rs, crates/tui/src/core/engine/tool_catalog.rs · confidence: high_

The building blocks exist but are not wired into a persona-keyed filter. Evidence: ToolRegistry::filter_by_capability(crates/tui/src/tools/registry.rs:264) is present but #[allow(dead_code)]; the parent turn builds ONE registry gated only by allow_shell (crates/tui/src/core/engine/tool_setup.rs:57-58, with_agent_tools(allow_shell)), not by an agent persona; sub-agents call with_full_agent_surface (registry.rs:960) then only optionally filter by an explicit allowed_tools list, and the role-default is the full registry (subagent/mod.rs:463-469 deprecation note: 'Default sub-agents inherit the full parent registry; pass an explicit allowed_tools list only for narrow Custom roles'). Plan: (1) add a Tool capability/role tag and a tools(role) accessor on ToolRegistryBuilder/ToolRegistry keyed on AgentRole; (2) construct the per-role surface directly from role->capability profiles instead of building the full surface and filtering after the fact; (3) cache the per-turn tool list at the turn_loop tool-catalog seam (crates/tui/src/core/engine/tool_catalog.rs:339 active_tools_for_step) so construction happens once per turn. This is the same capability-profile work called out in the #3096 epic (AC: 'Tool capability profiles are built directly for roles ... instead of building the full parent agent surface and filtering afterward'), and the owner explicitly slipped #426 to v0.9.0 (comment 2026-05-03). Recommend coordinating it under #3096 rather than landing standalone in 0.8.61.

### #3016 — Reasoning-content integrity (#861 RC1-RC4 audit + remainder). The core RC fixes AND the specific 'likely real remainder' (Kimi/Moonshot reasoning_content gap) are already implemented and tested on this branch. What remains is the audit close-out plus a few recorded per-dialect fixtures and the Codex thinking fixture; #861/#598 are still open. This is a verify-and-finish epic, not a quick fix.

_files: crates/tui/src/client/chat.rs, crates/tui/src/client/responses.rs, crates/tui/src/tui/streaming_thinking.rs, crates/tui/src/tui/ui.rs, crates/tui/src/models.rs · confidence: high_

Most of the issue's plan is already landed; treat this as a scoped verify+finish, not a fresh build. Done already: RC1/RC3 finalize-on-error paths call streaming_thinking::finalize_current before clearing (crates/tui/src/tui/ui.rs:1511,1685,4823,4880,4971; '#861 RC3' drain at ui.rs:1498); RC2 stash-before-reset in start_block (crates/tui/src/tui/streaming_thinking.rs:169-189,200); the Kimi/Moonshot gap the issue flagged as the real remainder is fixed: ApiProvider::Moonshot added to provider_accepts_reasoning_content with '#3016' comment (crates/tui/src/client/chat.rs:2060) and native bare ids handled in model_supports_reasoning (kimi- prefix + explicit kimi-k2.6/k2.7-code/kimi-for-coding at crates/tui/src/models.rs:374,389-394), with tests decoder_streams_moonshot_multi_chunk_reasoning_as_thinking (chat.rs:2924) and stream_classifies_moonshot_kimi_as_reasoning (chat.rs:3963). Remaining acceptance work: (1) recorded MULTI-chunk decode fixtures exist only for Moonshot/Minimax/OpenRouter (chat.rs:2924-3065) - add explicit recorded DeepSeek delta.reasoning_content and GLM/Qwen-on-vLLM (delta.reasoning) multi-chunk sequences in mod stream_decoder_tests; (2) responses.rs has NO recorded response.reasoning_text.delta thinking fixture (only effort-label tests at responses.rs:706-719) - add one asserting ThinkingDelta + a Thinking block replays as {type:'reasoning'} (build seam responses.rs:286-294,551-553); (3) extend crates/tui/tests/reasoning_content_replayed_after_tool_call.rs with a ThinkingComplete-lost case; (4) write per-RC close-out notes and close #861 (still OPEN), decide #598 (still OPEN). Keep work in mod stream_decoder_tests so `cargo test -p codewhale-tui stream_decoder` covers it.

### #3075 — Provider-aware /model picker that searches across the whole model catalog and lets a user select provider+model together is NOT implemented. The current picker has no search input, no typed-filter state, and builds rows only from the active provider plus a saved cross-provider tail.

_files: crates/tui/src/tui/model_picker.rs, crates/tui/src/config.rs · confidence: high_

Genuine feature work, not started. Evidence of current state: ModelPickerView::handle_key only handles Esc/Enter/Up/Down/PageUp/PageDown/Home with no KeyCode::Char/Backspace/Delete and no search-string field (crates/tui/src/tui/model_picker.rs:496-525); the doc comment still frames it as 'model and thinking' (model_picker.rs:1,6); rows come from model_completion_names_for_provider(app.api_provider) plus saved other-provider models as a tail (picker_model_rows_for_app at model_picker.rs:391-443), exactly the active-provider-first shape the issue calls out. Cross-provider apply already works (build_event sets provider when row.provider != initial_provider, model_picker.rs:218-223), so the apply path is reusable. Plan: (1) add search state (query String) to ModelPickerView and handle Char/Backspace/Delete/clear in handle_key; (2) rebuild visible rows from an all-provider catalog rather than only app.api_provider, deriving rows by iterating provider metadata + model_completion_names_for_provider(provider) + saved provider_models until the #3071/#3072 registry lands; (3) extend ModelPickerRow with provider display + provenance (catalog/saved/custom/auto) and a metadata hint; (4) add a 'Use "<query>" with <provider>' custom row when no catalog match; (5) tests for live filtering, cross-provider selection, custom-row, and preserving current model (cargo test -p codewhale-tui model_picker). Build seam is entirely within model_picker.rs (handle_key + picker_model_rows_for_app) plus config.rs:822 model_completion_names_for_provider.

### #3096 — Split sub-agents into a headless worker runtime with lightweight TUI projections. This is a large, owner-tracked architecture epic. v0.8.60 landed the substrate (AGENT_RUNTIME.md, fleet-backed exec subprocesses, AgentWorkerSpec/Record/Event ledger, nonblocking agent_eval), but the core acceptance — making default sub-agent execution a fleet-backed worker lifecycle instead of the session-shaped SubAgentRuntime, role tool profiles built directly, TUI as pure projection — remains.

_files: crates/tui/src/tools/subagent/mod.rs, crates/tui/src/fleet/worker_runtime.rs, crates/tui/src/fleet/scheduler.rs, crates/tui/src/fleet/ledger.rs, docs/AGENT_RUNTIME.md · confidence: high_

Large epic; keep tracked, do not attempt to complete in 0.8.61 (substrate is in, the remainder is multi-week). Already landed: docs/AGENT_RUNTIME.md (7.9KB, exists) defines one headless runtime; fleet workers spawn real headless execution via codewhale exec (crates/tui/src/fleet/worker_runtime.rs:1-82, fleet_task_to_worker_spec building an AgentWorkerSpec); persisted AgentWorkerSpec (crates/tui/src/tools/subagent/mod.rs:661) / AgentWorkerRecord (mod.rs:765) + fleet ledger (crates/tui/src/fleet/ledger.rs) + FleetWorkerRuntimeProjection (crates/tui/src/fleet/manager.rs:116-123); agent_eval no longer forces blocking joins for polling/follow-up (owner note 2026-06-14). Remaining (the umbrella): (1) replace the session-shaped SubAgentRuntime clone-bundle execution (mod.rs:830-886,1030-1075,1556-1560) with a minimal WorkerRuntime/scheduler boundary so default sub-agents run as fleet-backed workers; (2) build role tool profiles directly instead of with_full_agent_surface(...) then filter (mod.rs:5507-5535 / registry.rs:960) - shares the seam with #426; (3) make the TUI subscribe to worker events rather than own lifecycle state (crates/tui/src/tui/subagent_routing.rs, widgets/agent_card.rs); (4) scheduler-owned backpressure/concurrency. Build seams: crates/tui/src/fleet/{scheduler.rs,worker_runtime.rs,ledger.rs,manager.rs}, crates/tui/src/tools/subagent/mod.rs. Owner has split follow-up slices (#3142,#3154,#3159,#3178,#3097); coordinate there.

### #3205 — Route-effective model inventory + auto fleet model selector. The issue's 'safe initial slice' (authenticated-route inventory, DeepSeek-flash auto selector, auto failing clearly instead of resolving to a mismatched default) is largely landed already. The remaining acceptance — typed fleet role model policy (explicit/inherit/auto+constraints), `codewhale model list` unified on the inventory source, and selector reporting — is a dedicated follow-up slice the owner has explicitly carved out.

_files: crates/tui/src/model_inventory.rs, crates/tui/src/model_routing.rs, crates/config/src/lib.rs, crates/cli/src/lib.rs · confidence: high_

Foundation done; remainder is a scoped follow-up, so design (not already-done, not quick-fix). Already landed: route-effective inventory filtered to configured/authenticated providers (crates/tui/src/model_inventory.rs:1, tests inventory_includes_only_usable_authenticated_providers:266 and inventory_marks_local_providers_keyless:294); DeepSeek-flash style auto selector that picks from the inventory via a small non-thinking router call and falls back to a heuristic (crates/tui/src/model_routing.rs:553-610 auto_route_inventory_recommendation using inventory.router_model + reasoning_effort:'off', resolve_auto_route_with_inventory:417, resolve_explicit_route_with_inventory:456); FleetRolePreset config shape + built-in presets (crates/config/src/lib.rs:1183-1281). The immediate misroute bug (#3202) is fixed. Remaining acceptance per the issue + owner comment (2026-06-14 'the full issue is still bigger'): (1) a typed fleet role MODEL POLICY (explicit vs inherit-default vs open/auto-with-constraints fast/reasoning/tool-heavy/cheap/local/high-context) layered onto FleetRolePreset; (2) make `codewhale model list` (parsed at crates/cli/src/lib.rs:2238) consume the same ModelInventory source as fleet setup; (3) selector reporting + a clear failure when no DeepSeek route is configured for auto; (4) tests for mismatched default provider/model, multi-alias-across-providers, and role explicit override. Build seams: crates/tui/src/model_inventory.rs, crates/tui/src/model_routing.rs, crates/config/src/lib.rs (FleetRolePreset), crates/cli/src/lib.rs (model list). Coordinate with #3167/#3154.

### #3024 — Wire reasoning-effort/token-limit params for OpenAI, Moonshot, Ollama, Atlascloud. Atlascloud + Moonshot + Ollama are fully wired (steps 1,4,5 done), but the OpenAI o-series/gpt-5 pieces (steps 2 and 3) -- the part the issue calls 'broken' -- are still missing.

_files: crates/tui/src/client.rs, crates/tui/src/client/chat.rs, docs/PROVIDERS.md · confidence: high_

PARTIALLY done; the OpenAI half remains. Done: apply_reasoning_effort now mutates the body for Atlascloud (joined into the DeepSeek dialect groups at client.rs:1261,1318,1400), Moonshot (thinking enabled/disabled, client.rs:1287-1290,1363,1426 with '#3024' comments), and Ollama (think true/false, client.rs:1291-1294,1367,1430); the strict-no-op test was updated to drop them (client.rs:2683). NOT done (the broken-route part): (a) step 2 -- no openai_reasoning_model() helper exists anywhere (grep returns 0 hits in client.rs/chat.rs); the Openai arms are still bare no-ops at client.rs:1362 and 1425 ('ApiProvider::Openai | WanjieArk | OpenaiCodex => {}'), so o1/o3/o4/gpt-5 models never get reasoning_effort. (b) step 3 -- apply_provider_token_limit (chat.rs:62-71) still only swaps max_tokens->max_completion_tokens for XiaomiMimo, so OpenAI reasoning models still send max_tokens and get rejected. Implementation plan (4-6 lines): 1) Add `fn openai_reasoning_model(model:&str)->bool` in client.rs returning true for lowercased prefixes o1/o3/o4/gpt-5 (gpt-4o must NOT match). 2) In each Openai arm of apply_reasoning_effort, read body.get('model'); when it is a reasoning model map low->low, medium->medium, high/max->high; leave 'off' and non-reasoning/missing-model bodies unchanged (do not change the fn signature -- ~15 test call sites depend on it). 3) Extend apply_provider_token_limit: keep XiaomiMimo, additionally when provider==Openai && openai_reasoning_model(body['model']) remove max_tokens and set max_completion_tokens; export the helper pub(super) so chat.rs can import it (chat.rs imports from super at chat.rs:55-60). 4) Tests: add openai+o3-mini medium asserts reasoning_effort:'medium'; gpt-4o unchanged; off unchanged; chat.rs openai_reasoning_models_use_max_completion_tokens mirroring xiaomi_mimo_uses_max_completion_tokens_payload_key (chat.rs:4005). Build seams: client.rs:1362, client.rs:1425, client/chat.rs:62-71. Bigger than quick-fix (touches 2 files, ~5 match arms across 3 tiers, new helper, model-gating, plus PROVIDERS.md table) and 400-risk on arbitrary OpenAI-compatible endpoints, so design.

### #3076 — Alphabetize provider ordering in model/provider UI to de-emphasize DeepSeek-first. Not started: ProviderKind::ALL still leads with Deepseek, no providers_sorted_for_display() helper exists, and the model picker still lists the active provider first.

_files: crates/config/src/provider.rs, crates/config/src/lib.rs, crates/tui/src/tui/model_picker.rs · confidence: high_

NOT done on this branch. Evidence of current state: ProviderKind::ALL still starts Deepseek, NvidiaNim, Openai, Atlascloud... (crates/config/src/lib.rs:158-184, the [Self;24] array) and all_providers() returns that insertion order directly (crates/config/src/provider.rs:527-535). No display-ordering helper exists (grep for 'sorted_for_display'/'display_order'/alphabet returns 0 hits). The model picker sorts only the cross-provider tail by provider key and keeps the active provider's rows first (model_picker.rs:416-433); there is no all-provider alphabetical browse mode. Implementation plan (4-6 lines): 1) Add `pub fn providers_sorted_for_display() -> Vec<&'static dyn Provider>` in provider.rs that clones all_providers() and sorts by display_name() (the Provider trait already exposes display_name(), provider.rs:47); keep ProviderKind::ALL untouched for stable internal parsing/defaults. 2) Point /model and any provider picker/completion surfaces at the new helper instead of ALL/all_providers(); keep the active provider highlighted/pinned but not hard-first. 3) Add a config-crate test asserting display order != ProviderKind::ALL order and is alphabetical, plus a model_picker test that browsing is alphabetized. Build seams: crates/config/src/provider.rs:527-535, crates/config/src/lib.rs:158-184, crates/tui/src/tui/model_picker.rs:416-445. Labeled cosmetic/cleanup but spans config + tui + a new public API + tests across multiple pickers, and intersects unmerged provider-aware search (#3075); treat as a small design item rather than an unattended quick-fix.

### #3207 — Binary requires GLIBC_2.39 (Ubuntu 24.04) and fails on Ubuntu 22.04 (glibc 2.35). Root cause confirmed: all Linux gnu targets build natively on ubuntu-latest (now 24.04), baking in a glibc 2.39 floor; no older-glibc or musl asset is produced.

_files: .github/workflows/release.yml, .github/workflows/nightly.yml · confidence: high_

NOT addressed; root cause confirmed in the release workflow. The x86_64-unknown-linux-gnu (and tui) targets run on `os: ubuntu-latest` (.github/workflows/release.yml:113-116,138-141) which GitHub now maps to Ubuntu 24.04 (glibc 2.39), and the build is a native `cargo build --release --target ...` (release.yml:227) -- so the produced ELF requires GLIBC_2.39, exactly the reporter's error. There is no musl, container, or zigbuild path (grep for musl/container/zigbuild/manylinux in release.yml: 0 hits beyond cross arm64/riscv pkgconfig). Options (pick one, each is real release-infra work, 4-6 lines): (A) Lowest-risk for compatibility: pin the x64 (and ideally arm64) gnu jobs to `os: ubuntu-22.04` so the glibc floor drops to 2.35, covering 22.04+ users; verify the libdbus-1-dev install step (release.yml:181) still resolves on 22.04. (B) Provide an additional fully-static `x86_64-unknown-linux-musl` asset (add matrix rows + `rustup target add` + musl-tools/`apt-get install musl-tools`), which removes the glibc dependency entirely but needs vendored-openssl/dbus feature audit. (C) Build x64 inside an older-glibc container or via cargo-zigbuild with `--target x86_64-unknown-linux-gnu.2.28`. Also update install docs once chosen. Build seams: .github/workflows/release.yml:108-161 (matrix), :173-181 (linux deps), :217-227 (build); mirror into .github/workflows/nightly.yml:28-61. Recommend (A) as the smallest reliable 0.8.61 fix, but it changes release infra and asset ABI, so it is design/owner-decision, not an unattended quick-fix.

### #2666 — Agent-visible, in-band resource telemetry (token budget, context pressure, child-agent status, cost) during long turns is not delivered. Rich accounting infrastructure exists (context_report.rs PromptSourceMap with budget_used_percent, core/capacity.rs controller, compaction token estimators) and a user-pull /context report|json|summary command, but nothing pushes live pressure/cost/child status to the model itself during a run. The issue is specifically about agent self-awareness, not a single tool failure, and it explicitly leaves acceptance criteria to be defined after inspecting these surfaces.

_files: crates/tui/src/context_report.rs, crates/tui/src/core/capacity.rs, crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/prompts.rs, crates/tui/src/commands/groups/debug/debug.rs · confidence: high_

Build seams already present: context_report::build_context_report / format_context_summary (crates/tui/src/context_report.rs, consumed by crates/tui/src/commands/groups/debug/debug.rs:142-148 for the /context command; headless variant at crates/tui/src/main.rs:4006-4007) computes total/active estimated tokens, context_window_tokens, and budget_used_percent. core/capacity.rs already classifies low/medium/severe pressure. The per-turn injection point is the runtime_prompt tag: crates/tui/src/core/engine/turn_loop.rs:2447 messages.push(self.runtime_prompt_message()). Plan: (1) extend runtime_prompt_message to optionally append a compact, append-only context/cost/child-agent line (budget_used_percent bucket, running sub-agent count) sourced from context_report + the subagent cache; (2) gate it behind a setting and keep it volatile-content-last so it does not bust the prefix cache (see the Volatile-content-last invariant on system_prompt_for_mode_with_context_and_skills in prompts.rs); (3) reuse capacity.rs thresholds rather than inventing new ones; (4) define acceptance criteria (which signals, what cadence, cost-known vs cost-unknown wording) as step 1 since the issue defers them. This is real cross-cutting work (engine turn loop + prompt cache invariants + new UX), not a hotfix.

### #3025 — An auto-clear / expiration policy for terminal sub-agent cards is not implemented. The branch has the closely-related foundation — reconcile_subagent_activity_state prunes agent_progress to only running agents, and the sidebar auto-collapses 'done' agents' detail lines — but none of the issue's specific requirements exist: no TTL/grace-period removal of completed cards from subagent_cache, no bounded retention under burst, no pin/keep control, no separate failed-vs-completed retention, no manual 'clear completed now'.

_files: crates/tui/src/tui/subagent_routing.rs, crates/tui/src/tui/ui.rs, crates/tui/src/tui/sidebar.rs, crates/tui/src/tui/app.rs · confidence: high_

Where it stands: crates/tui/src/tui/subagent_routing.rs:46-74 reconcile_subagent_activity_state retains agent_progress only for Running ids and clears agent_activity_started_at when none run, but it does NOT prune app.subagent_cache (completed/failed/interrupted SubAgentResults persist). crates/tui/src/tui/ui.rs:2418-2425 (AgentList handler) rebuilds subagent_cache from the snapshot, filtering only from_prior_session. The sidebar at crates/tui/src/tui/sidebar.rs:2194-2199 hides detail lines for status=='done' but keeps the card. Plan (4-6 lines): (1) add a terminal-card retention model to App (timestamp + pinned flag) keyed on agent_id alongside subagent_cache (crates/tui/src/tui/app.rs:1543); (2) on AgentComplete/terminal transition (ui.rs:2368-2425) stamp completed_at and enforce a grace TTL + a max-retained bound, with failed/interrupted given a longer documented retention; (3) filter expired/unpinned terminal cards in subagent_view_agents before update_subagents; (4) add manual controls (clear-completed, pin) via a /subagents action or keybinding; (5) keep transcript/result reachable through existing subagent_card_index/detail handles; (6) tests per acceptance: completed/failed/interrupted/pinned/running. Cross-cuts app state, the AgentList/AgentComplete handlers, sidebar, and the subagent view — needs a small design, not an unattended patch.

### #3146 — The full Cursor-style activity timeline is not built; what exists is the narrow foundation the issue itself describes ('a first narrow local slice'). detect_tool_runs collapses contiguous successful, safe tool runs into one muted summary ('Explored N files, M searches', 'ran N commands: families', 'edited N files', 'Updated metadata') with a safety guard that keeps failed/destructive/edit/commit/push/review tools visible. Missing per the acceptance criteria: edit-group diff stats (+X -Y), optional per-file edit rows, in-flight 'Running <purpose>' rows, and 'Waiting for N commands to finish' rows.

_files: crates/tui/src/tui/history.rs, crates/tui/src/tui/transcript.rs, crates/tui/src/tui/ui.rs, crates/tui/src/tui/mouse_ui.rs · confidence: high_

Foundation: crates/tui/src/tui/history.rs:837-895 detect_tool_runs/detect_tool_runs_from_slices group contiguous collapsible cells; is_collapsible_tool_cell (:912) only collapses tool.is_success() && !is_collapsible_guard, and generic_tool_name_is_collapse_guard (:916-933) excludes patch/write/edit/delete/remove/commit/push/review — so dangerous/failed actions stay prominent (satisfies the safety non-goals). tool_run_summary (:1029-1075) emits the muted summary; ToolRunActivity classification at :963-1025. Expansion/detail paths exist (expanded/collapsed_cells in ui.rs/mouse_ui.rs; Alt+V detail pager). What to build (4-6 lines): (1) extend ToolRunActivitySummary (history.rs:797-825) to accumulate insertions/deletions from edit/patch cells (PatchSummary/DiffPreview carry diff stats) and render 'Edited N files +X -Y'; (2) add optional per-file edit rows behind the existing expansion affordance; (3) add non-terminal run states so in-flight groups render 'Running <purpose> <families>' and background/long waits render 'Waiting for N commands to finish'; (4) wire these summary rows into the live transcript renderer (transcript.rs) as muted metadata cells rather than generic tool cards while preserving the expand-to-original-cells path; (5) tests for command/edit summaries, pluralization, expansion, detail targeting, and failure/running guards. This is a multi-surface UI feature (history model + transcript render + expansion) the owner already retargeted as 'a full activity-timeline feature' — design/epic, not a hotfix.

### #891 — Codex-style /goal long-running task mode. Tracker/design issue; goal mode is substantially built but the full autonomous lifecycle unification is owned by hub issue #3215. Owner comment dated today (2026-06-14) explicitly pulls this into v0.8.61 via #3215 and uses #891 as the acceptance sweep, not as fresh standalone work.

_files: crates/tui/src/tools/goal.rs, crates/tui/src/commands/groups/project/goal.rs, crates/tui/src/core/engine/turn_loop.rs, crates/core/src/lib.rs, crates/state/src/lib.rs · confidence: high_

Do NOT treat as standalone work; it is the acceptance/reference tracker for #3215. Goal mode already exists across all layers: /goal command lifecycle set/clear/pause/resume/complete/blocked+budget (crates/tui/src/commands/groups/project/goal.rs:10-139), model-visible runtime tools create_goal/get_goal/update_goal with mandatory completion evidence (crates/tui/src/tools/goal.rs:241-461), bounded continuation scheduler with runaway guard MAX_GOAL_CONTINUATIONS_PER_TURN=3 (crates/tui/src/core/engine/turn_loop.rs:2390-2437), engine SharedGoalState sync (crates/tui/src/core/engine.rs:2680-2701), TUI bridge apply_goal_snapshot_to_app (crates/tui/src/tui/ui.rs:5799), and durable ThreadGoalRecord storage (crates/state/src/lib.rs:218,525). Build seam for the remaining autonomous-loop + persistence unification: wire /goal through crates/core/src/lib.rs:650 set_thread_goal (currently only reached by the app-server/exec path, not the interactive TUI command). Implementation plan: (1) add a goal-state bridge translating app.hunt <-> tools::goal::GoalState <-> ThreadGoalRecord; (2) make /goal persist via that bridge; (3) add a cross-turn continuation scheduler beyond the in-turn guardrail; (4) verify acceptance criteria from #3215; (5) link/close 891 against #3215.

### #1976 — Goal mode persistent objective/workflow surface (originally v0.8.43). Same goal-mode family as #891. Owner de-milestoned it into #2058, then on 2026-06-14 pulled it forward into v0.8.61 and linked the concrete implementation plan to hub #3215; it now serves as the UX/design reference, not independent code.

_files: crates/state/src/lib.rs, crates/core/src/lib.rs, crates/app-server/src/lib.rs, crates/tui/src/tui/ui.rs, crates/tui/src/commands/groups/project/goal.rs · confidence: high_

Same disposition and seams as #891: the concrete work lives in hub #3215 (unify the durable goal contract so /goal, runtime goal tools, the ThreadGoalRecord state store, and the TUI projection share one source of truth). Evidence the pieces already exist but are NOT yet unified: durable persistence get/set/clear live only in crates/core/src/lib.rs:650-680 (upsert_thread_goal) and the app-server endpoints thread/goal/set|get|clear (crates/app-server/src/lib.rs:699-721, round-trip test at 1179), while the interactive /goal writes only to app.hunt (crates/tui/src/tui/ui.rs:892-925,5762-5764) and runtime GoalState. The bridge between them is the missing work. Recommend keeping 1976 as a design reference linked to #3215 and not scheduling separate implementation.

### #3083 — Make /provider a capability/readiness dashboard instead of a hard-coded key picker. Unbuilt: the picker still renders from a static provider list with hard-coded hints and shows none of the resolved model, capabilities, context/max-output, pricing provenance, or readiness. The backing capability data already exists but the picker does not consume it.

_files: crates/tui/src/tui/provider_picker.rs, crates/tui/src/config.rs, crates/tui/src/commands/groups/core/provider.rs · confidence: high_

Real UX work needing a row-model design; the data seam is ready, the presentation layer is not. Current state: crates/tui/src/tui/provider_picker.rs (755 lines) builds rows from ApiProvider::all()+has_api_key_for with hard-coded env_var_for (:102) and provider_hint (:130); it shows only key state. Data already available to feed a row model: provider_capability returns context_window, max_output, thinking_supported, tools/cache/streaming flags (crates/tui/src/config.rs:361-372,415-519) and is already surfaced via codewhale doctor --json provider_capability_report. Plan: (1) add a ProviderRowModel/ProviderReadiness builder (new module beside provider_picker.rs) fed by config + auth state + provider_capability + model metadata + cached health, testable without rendering; (2) replace the hard-coded hint/env tables with row-model data; (3) render readiness + current resolved model + provenance (default/saved/custom/catalog) + capability/metadata badges, labeling unknown pricing/context explicitly; (4) add row actions switch/edit-key/edit-base-url/edit-model/test-connection/open-/model-filtered; keep network probes cached/explicit so opening /provider never blocks. Tests per provider category (hosted-key, OAuth, optional-key local, custom base URL). Build seams: provider_picker.rs:102,130,236,311 and config.rs:415.

### #3211 — First-class permission profiles (read_only/workspace/full_access) plus an EffectivePermissions layer, an unambiguous ApprovalPolicy, an ExecutionPolicy, a /permissions picker, and nonblocking execution defaults. None of this abstraction exists yet; mode still directly couples allow_shell/trust_mode/approval_mode.

_files: crates/tui/src/tui/app.rs, crates/tui/src/config.rs, crates/tui/src/tools/registry.rs, crates/tui/src/commands/groups/core/mod.rs · confidence: high_

Real design/epic work: cross-cutting through config, app state, tool registry, and prompt. Confirmed unbuilt: grep for PermissionProfile/EffectivePermissions/ApprovalPolicy/ExecutionPolicy and a /permissions command returns nothing in crates/. Current coupling: App::set_mode mutates allow_shell/trust_mode/approval_mode on YOLO transitions (crates/tui/src/tui/app.rs:2526-2543), and Plan mode strips shell from the catalog. Plan: (1) add PermissionProfileId + EffectivePermissions types beside Settings/Config (crates/tui/src/config.rs) and derive existing allow_shell/trust_mode/approval_mode/sandbox_mode from a profile during migration; (2) add ApprovalPolicy with unambiguous labels mapping legacy approval_mode auto|suggest|never for backcompat; (3) make ToolRegistryBuilder consult EffectivePermissions instead of raw AppMode+allow_shell for catalog/shell inclusion (crates/tui/src/tools/registry.rs:275,513), so Plan can run read-only diagnostics; (4) add /permissions picker (Read Only/Workspace/Full Access with a Full-Access confirm) and a /config profile row; (5) keep project overlays monotonic (tighten only). Coordinate with sibling nonblocking-exec issues (#3212/#3213). Build seams: app.rs:2526, config.rs (Settings), registry.rs:275/513.

### #2027 — Route low-risk tool-heavy scout work (GitHub triage, read-only search, status polling) to Flash by default, keep Pro for synthesis/high-risk, with a visible route receipt and conservative/aggressive config. Partial substrate exists but the scout classifier and route receipt do not.

_files: crates/tui/src/model_routing.rs, crates/tui/src/tools/subagent/mod.rs, crates/tui/src/tui/subagent_routing.rs · confidence: high_

Not done as specified. What exists: a prompt-COMPLEXITY auto-router (crates/tui/src/model_routing.rs:344 resolve_auto_route_with_flash, AutoRouteSource::FlashRouter, cost_saving bias at :98-152) and sub-agent auto_model that can pick the cheap tier (crates/tui/src/tools/subagent/mod.rs:6090-6102 applies recommendation.model when runtime.auto_model; provider_router_candidates exposes cheap=deepseek-v4-flash at model_routing.rs:33/68). Missing per the issue: (1) a WORK-SHAPE scout classifier (GitHub triage, read-only search batches, status/PR inventory, large-log first pass, verification-only) distinct from prompt complexity; (2) a persisted route receipt explaining parent/child model + effort + why + cost-saving vs quality-preserving; (3) explicit Pro-gating of high-risk actions (merge/push/release/destructive) that overrides a Flash scout; (4) conservative/aggressive config knob; (5) tests over the five enumerated route classes. Build seams: add a scout-classifier fn next to auto_model_heuristic_with_bias in model_routing.rs:98; thread its decision through subagent route selection at subagent/mod.rs:6087-6104 (where route.model/reasoning_effort are set); surface the receipt in crates/tui/src/tui/subagent_routing.rs (already renders Model: per task at :327). Multi-surface feature, not a quick fix.

### #2917 — After renaming deepseek-tui -> codewhale, running `deepseek update` fails with 'failed to spawn codewhale ... not found on PATH or in sibling directory'. The new migration guidance only fires on `codewhale update` from a deepseek-tui binary, not on the legacy deepseek/shim path when codewhale is absent.

_files: crates/cli/src/bin/codew_legacy_shim.rs, crates/cli/src/update.rs, crates/cli/src/lib.rs · confidence: high_

Partially addressed, but the exact reported path is not. The error string in the report comes from crates/cli/src/bin/codew_legacy_shim.rs:19-24 (spawn_codewhale error) and :46-49 ('codewhale not found on PATH or in sibling directory'). update.rs already has a legacy guard (is_legacy_binary at update.rs:196 + legacy_binary_message at :205 with full npm/cargo/brew reinstall instructions) but it only triggers when a binary NAMED deepseek* runs `codewhale update` (update.rs:32-33 bails before updating). The user ran `deepseek update`, which the shim forwards to a codewhale that is not installed, so they hit the bare shim NotFound message with no reinstall guidance. Design fix: (a) make the shim NotFound branch emit the same migration guidance as legacy_binary_message (reinstall via original method) instead of just 'not found on PATH'; (b) decide product behavior for `deepseek <subcommand>` when codewhale is absent (the shim is the permanent codew alias per its header, so a deepseek-named copy is the migration artifact). Verify by simulating current_exe with no sibling/PATH codewhale and asserting the message names the reinstall path. Touches codew_legacy_shim.rs plus a shared message helper reused from update.rs/lib.rs. Design (not quick-fix) because the binary-naming/migration matrix (codewhale vs codewhale-tui vs deepseek shim) needs a deliberate decision, not a string swap.

### #3212 — Make independent shell/verifier work default to background jobs with nonblocking waits and automatic completion notification. Substrate exists (background shell, task_shell_start, detached verifiers, cancel-leaves-running), but the central ask -- flip exec_shell_wait to nonblocking-by-default and reframe wait vs join -- is not done.

_files: crates/tui/src/tools/shell.rs, crates/tui/src/tools/subagent/mod.rs, crates/tui/src/tui/shell_job_routing.rs · confidence: high_

Substantial substrate present, but the defining behavior change is unshipped, so not a quick-fix. Already done: exec_shell background=true returns a task id immediately (crates/tui/src/tools/shell.rs:2058-2060 schema + :2126), task_shell_start, cancelled wait leaves the job running (ShellWaitTool.execute wait_canceled branch ~shell.rs:2898-2918 and the 'leaves the background task running' description at :2856), detached background verifiers (commit bceba7c23), and nonblocking sub-agent eval/steering (commit 5ca618d70). NOT done, and explicitly requested: exec_shell_wait still defaults wait=true (shell.rs:2894 optional_bool(&input,'wait',true); schema says 'default: true' at :2871-2873). The issue wants the safer default to be nonblocking (wait=false) or a canonical nonblocking read path, plus tool-description nudges that waits inspect-by-default while blocking requires an explicit block/wait=true, plus automatic-completion transcript events and tests for wait-default/parallel-read-only scenarios. Build seams: ShellWaitTool input_schema + execute (shell.rs:2868-2900), the background-completion event plumbing into the engine transcript, and the timeout recovery-hint text that should prefer 'restart in background and continue'. Design deliberately: flipping the default alone is risky-by-blast-radius (many flows assume blocking waits), so the description/notification work must land with it; also overlaps #3211 permission profiles and #3213 capability prompt.

### #2029 — Sub-agent checkpoint/continue across turns. Substrate is largely landed (Interrupted state + continuable SubAgentCheckpoint + agent_eval resume); remaining acceptance criteria are a real epic.

_files: crates/tui/src/tools/subagent/mod.rs, crates/config/src/lib.rs · confidence: medium_

Substantially implemented on this branch, but the issue's full acceptance set is an epic, not a quick close. Already present: crates/tui/src/tools/subagent/mod.rs:1098-1109 `SubAgentCheckpoint{continuation_handle, continuable, reason, steps_taken, messages}`; :593 distinct `SubAgentStatus::Interrupted(String)` (not Failed); :2617-2627 timeout/interrupt path sets Interrupted + emits worker status; :2754-2761 `subagent_checkpoint_is_continuable`; :3565,3599-3640 `agent_eval` resumes via `continue_from_checkpoint` replaying saved messages; :846/892/2813 projection accepts `interrupted_continuable`; :3706-3707 `timed_out` flag surfaced; api_timeout configurable via `[subagents] api_timeout_secs` (config). Gaps vs issue: (1) literal `needs_continuation`/`timed_out_with_checkpoint` UI status names — code models this as Interrupted + continuable=true + timed_out flag; reconcile the naming/UI; (2) per-`[subagents]` `max_runtime`/`max_turns` (only api_timeout_secs exists; max_turns is fleet-level in config/src/lib.rs:1130); (3) an end-to-end test asserting checkpoint→timeout→resume→complete without losing the summary. Owner's 2026-06-14 comment ties remaining work to #3216/#3217. Build seams: subagent/mod.rs:2617 (timeout→checkpoint), :3599 (resume), :2754 (continuable projection).

### #3085 — Provider+model SKU pricing engine with provenance/unknown-states/UI integration. The #3066 quick fix landed; this deeper architecture is net-new.

_files: crates/tui/src/pricing.rs, crates/tui/src/models.rs, crates/tui/src/client.rs · confidence: high_

Real architecture work, not on branch. #3066 (its immediate-fix sibling) is done, but pricing.rs is still the model-name substring table this issue wants replaced: lookup is keyed on model string alone (pricing.rs:146 `pricing_for_model_at(model, ...)`, no provider id), there is no provenance/`last_verified`/`valid_from` metadata, no explicit `PricingUnknown` reason type (it returns `Option`/`has_pricing_for_model` bool), and no `doctor --json` pricing-provenance surface. Implementation plan: (1) add a provider+model-keyed pricing catalog type with token classes (input/output/reasoning/cached/cache-write), currency + display fallback, provenance enum, and staleness timestamps; (2) change the lookup signature to take provider id + model + optional route; (3) normalize provider usage payloads into canonical billable units before pricing (client.rs already maps `cached_tokens`→`prompt_cache_hit_tokens`); (4) render explicit unknown/n-a + source in sidebar, /model, /provider, and doctor --json; (5) tests for DeepSeek cache pricing, USD-only, unknown-local, stale, CNY fallback. Build seams: crates/tui/src/pricing.rs:116/146 (lookup + has_pricing), crates/tui/src/models.rs:250-279 `known_context_window_for_model` (the catalog match-table precedent to mirror), crates/tui/src/client.rs:1403-1410 (usage normalization). Depends on #3071/#3072 (model metadata registry / catalog hydration). Multi-PR epic.

### #3167 — Agent Fleet org chart + Fleet setup modal/role picker. Config substrate (FleetRolePreset + built-in presets) exists; the retargeted product scope (UI modal, provider/model picker, persisted role map, calmer palette) is absent.

_files: crates/tui/src/config_ui.rs, crates/config/src/lib.rs · confidence: high_

Config layer is partly there but the retargeted product scope is unbuilt. Present: crates/config/src/lib.rs:1183 `FleetRolePreset{description, tool_profile, tools, capabilities, timeout_seconds, trust_level}`, :1089 `roles: BTreeMap<...>` on the fleet config, :1232 `resolve_role`, :1242 `built_in_role_presets()` (smoke-runner/reviewer/builder/read-only), plus FleetExecConfig trust/depth hardening. MISSING (the owner's 2026-06-14 retarget makes this product scope, not docs): crates/tui/src/config_ui.rs has zero fleet/role/party/picker code (grep empty across its 1311 lines), there is no `/fleet party` or `/fleet roles` command, no provider/model picker reusing configured providers, no persisted role->provider/model/persona/tool-profile map written from the UI, and no calmer-palette pass. Implementation plan: (1) add a Fleet setup modal in config_ui.rs reachable from config + `/fleet party`/`/fleet roles`; (2) enumerate already-configured providers/models (no secret entry) as per-role selections; (3) persist the role->profile map into `[fleet.roles]` via the existing FleetRolePreset/resolve_role path; (4) let task specs reference these role profiles; (5) tone down selection/warning (yellow) colors. Build seams: crates/config/src/lib.rs:1183/1232/1242 (persistence + presets), crates/tui/src/config_ui.rs (modal host). Multi-PR product work; verify with `cargo test -p codewhale-tui fleet_roles` once role config UI lands.

### #3213 — Split model-facing runtime capabilities from human-facing mode labels. Unimplemented: the per-turn tag still leaks mode="yolo"/approval="auto"/allow_shell.

_files: crates/tui/src/core/engine.rs, crates/tui/src/prompts.rs, crates/tui/src/core/engine/tests.rs · confidence: high_

Net-new, no implementation on branch. The exact branding the issue wants removed is still emitted: crates/tui/src/core/engine.rs:2747-2765 `runtime_prompt_text(mode, approval_mode, allow_shell)` formats `<runtime_prompt visibility="internal" mode="{yolo|agent|plan}" approval="{auto|suggest|never}" allow_shell="{bool}"/>`; the frozen reference at crates/tui/src/prompts.rs:755-761 still teaches the model the `mode=`/`approval=` protocol. There is no `cw_runtime_capabilities` tag, no `EffectivePermissions` struct (grep empty; permissions flow through `build_tool_context`/ToolContext in engine), and no test forbidding `YOLO`/`Plan`/`Agent`/`approval="auto"`. Implementation plan: (1) replace runtime_prompt_text with a function deriving a compact capability tag (writes/shell/network/approvals/execution/planning) from the effective tool context + approval policy instead of UI labels; (2) update render_runtime_policy_reference (prompts.rs:746) to document capability fields, not mode/approval branding; (3) keep AppMode labels in header/footer/`/status`/`/mode`/`/permissions` only; (4) ensure child/fleet worker runtime messages inherit capabilities (subagent runtime); (5) add engine tests asserting the per-turn tag omits the four banned tokens while plan/agent posture still holds. Build seams: engine.rs:2747 + its caller :1532, prompts.rs:746, plus the tool-context/approval mapping the tag must read from. Design (frozen-prompt + cache-invalidation sensitivity makes it non-trivial).

### #1186 — Typed persistent permission rules. The ask-only foundation has landed on this branch, but the full issue (typed allow/deny decisions + deny>ask>allow precedence + legacy auto_allow/auto_deny mapping) is explicitly NOT implemented and is a multi-PR epic per the issue's own comment thread.

_files: crates/config/src/lib.rs, crates/execpolicy/src/lib.rs, crates/execpolicy/src/bash_arity.rs · confidence: high_

PARTIAL/foundation done: config/src/lib.rs:381-398 defines PermissionsToml{rules: Vec<ToolAskRule>} + ruleset(); execpolicy/src/lib.rs:31,66,311,504 implements Ruleset.ask_rules, with_ask_rules(), matching_ask_rule(), ask_rule_specificity(); config tests lib.rs:4075-4111 prove typed ask rules parse AND that allow/deny shapes are deliberately rejected ("permissions.toml should be ask-only in this slice"). The remaining work is a real design/epic: (1) extend ToolAskRule (or add a sibling type) with a `decision` enum allow/deny/ask at execpolicy/src/lib.rs:79; (2) implement deny>ask>allow precedence in matching_ask_rule at lib.rs:311 with tests; (3) map legacy auto_allow/auto_deny (bash_arity.rs:263) into exec_shell decision rules for back-compat; (4) reuse bash arity matching + workspace-relative glob for file paths. The issue thread (greyfreedom + maintainer) already sequences this as ask-only -> approval-flow -> typed allow/deny follow-ups, so scope it as the next slice, not a quick fix.

### #2054 — Composer can accidentally enter queued-steer edit state without clear mode feedback. A substantial state-labeling foundation already exists, but several acceptance criteria (row-level send-now/drop/clear affordances, guarding the single-key path into edit, explicit live-steer vs queued-follow-up distinction in the label) are not fully met. Labeled bug+question.

_files: crates/tui/src/tui/widgets/pending_input_preview.rs, crates/tui/src/tui/composer_ui.rs, crates/tui/src/tui/ui.rs, crates/tui/src/tui/app.rs · confidence: medium_

PARTIAL: pending_input_preview.rs already distinguishes states via QUEUED_MESSAGE_PREFIX ("Queued follow-up:", line 29) and PENDING_STEER_PREFIX ("Steer pending:", line 27), renders a "Pending inputs" header (line 117) and "edit last queued message" hint (line 169), and has a test editing_queued_message_renders_explicit_state_and_restore_hint (line 394). composer_ui.rs:17-36 next_escape_action returns DiscardQueuedDraft and app.rs has cancel_queued_draft_edit (ui.rs:3844). Remaining design work: (1) audit whether cancel_queued_draft_edit restores the prior normal draft or silently drops it (AC: "does not silently drop unrelated draft text") around app.rs queued_draft handling; (2) add row-level affordances/commands for send-now/drop/clear on Pending inputs rows (no "send now"/"drop queued" handlers exist today, grep is empty); (3) make the single-key path into "edit last queued message" (the Up binding, ui.rs:7310 "Queued: ... — ↑ to edit") only fire when the state is visually obvious; (4) add a repro test for accidentally entering queued-draft edit while a turn is loading. Build seams: crates/tui/src/tui/widgets/pending_input_preview.rs:27-179, crates/tui/src/tui/composer_ui.rs:17-36, crates/tui/src/tui/ui.rs:3844,7310,7349.

### #3188 — Show the actual Git repo/workspace identity in the TUI status surface. Genuine gap: the status surface currently shows only branch+status (no repo name, no cwd-basename / no-git-repo fallback, no remote in detail). Well-scoped but exceeds the unattended quick-fix bar (new render logic + git detection + truncation + 5 tested states).

_files: crates/tui/src/tui/workspace_context.rs, crates/tui/src/tui/footer_ui.rs, crates/tui/src/tui/context_inspector.rs · confidence: high_

DESIGN (moderate, clear seams). Today the footer chip shows only the branch: footer_ui.rs:621-633 footer_git_branch_spans reads app.workspace_context and calls workspace_context::branch_from_context; workspace_context.rs:97-122 collect() builds "branch | status" and returns None when the workspace is not a git repo (so nothing renders), and branch() at line 129-141 already handles detached HEAD ("detached:<hash>"). Plan: (1) extend workspace_context.rs collect() to also capture repo identity — repo name from `git rev-parse --show-toplevel` basename, optional remote URL from `git remote get-url origin` — and a non-git fallback to the cwd basename / "no git repo"; store as structured fields rather than only the "branch | status" string (today it is a flat String, ui.rs:6962). (2) Render "Repo: <name>" (and "<name> @ <branch>" when width allows) in the footer/status at footer_ui.rs:594-633, sourced strictly from git detection, never model/config text. (3) Add width-aware truncation that keeps identity over status and exposes full path/remote in the context inspector detail (context_inspector.rs:140 already prints workspace_context). (4) Tests covering git repo, worktree, detached HEAD, non-git cwd, and narrow-width truncation (mirror existing workspace_context tests). Build seams: crates/tui/src/tui/workspace_context.rs:97-141, crates/tui/src/tui/footer_ui.rs:594-633, crates/tui/src/tui/context_inspector.rs:140.

### #3214 — Post-merge branch hygiene checks for release/scratch branches. No existing tooling: no branch-prune script under scripts/release/ and no hygiene step in the release runbooks. Needs a new dry-run prune tool plus a contributor-preservation policy and runbook docs.

_files: scripts/release/, docs/RELEASE_CHECKLIST.md, docs/RELEASE_RUNBOOK.md · confidence: high_

DESIGN: there is no current implementation — `ls scripts/release/` (check-published.sh, check-versions.sh, prepare-release.sh, publish-crates.sh, verify-workspace-version.sh, etc.) has nothing branch-related, and grepping docs/RELEASE_RUNBOOK.md / RELEASE_CHECKLIST.md / OPERATIONS_RUNBOOK.md for branch/prune/fast-forward/renovate/stale returns no hygiene step. The live repro is real (the working checkout is on codex/v0.8.61 with ~40 stale codex/* and renovate branches present per `git branch -a`). Plan: (1) add scripts/release/branch-hygiene.sh (or .py) that reports: current checkout branch, local+remote release branch tips, and main, and verifies they agree after an integration merge; (2) detect local/remote branches whose HEAD is already contained in origin/main or the release branch (`git branch --merged`, `git cherry`) and list them as safe deletes; (3) enforce the contributor-preservation policy — never auto-delete a branch with unique non-Hunter commits unless merged/credited/preserved, and name branch+unique-commits+author+keep-reason in the report; (4) default to dry-run and require an explicit flag to delete; (5) add a runbook step in docs/RELEASE_CHECKLIST.md (after section 5 "Branch and PR", line 77) documenting the exact commands. Build seams: scripts/release/ (new file), docs/RELEASE_CHECKLIST.md:77. Sizable enough (new script + policy logic + docs + tests) to warrant a design slice rather than an unattended quick-fix.

### #1541 — Scaffolding for non-user cancel reasons exists (CancelReason enum + cancel_with_reason), but the actual acceptance criteria — migrating runtime/task-manager/preempt/teardown call sites to stamp External/Preempted/Internal — is NOT done. Every non-user path still calls plain .cancel().

_files: crates/tui/src/core/engine.rs, crates/tui/src/core/engine/handle.rs, crates/tui/src/core/engine/approval.rs, crates/tui/src/runtime_threads.rs, crates/tui/src/task_manager.rs · confidence: high_

The enum CancelReason{User,External,Preempted,Internal} and EngineHandle::cancel_with_reason are already in place (crates/tui/src/core/engine.rs:457-481; crates/tui/src/core/engine/handle.rs:32-42), and approval.rs reads the latched reason. But all non-user call sites still drop the reason: runtime_threads.rs:1832 (active_thread.engine.cancel()), the Op::CancelRequest handler at engine.rs:1178 (self.cancel_token.cancel() directly, bypassing the latch), task_manager.rs:810/949, runtime_api.rs:583, and tools/subagent/mailbox.rs:226. Build seams: (a) route runtime-API/task-manager stops through cancel_with_reason(External); (b) make the new-turn preempt path stamp Preempted; (c) make Op::CancelRequest / engine drop stamp Internal where distinguishable from a user Esc. Plan (4-6 lines): 1) Audit each .cancel()/cancel_token.cancel() site and tag its semantic origin. 2) Thread CancelReason through the runtime-API DELETE handler and task_manager stop into the EngineHandle. 3) For the engine-internal Op::CancelRequest path, decide User vs Internal by inspecting whether the request was operator-driven. 4) Add tests asserting approval.rs surfaces the right describe() string per path. This needs a design pass because the reason must be plumbed across the runtime_threads/task_manager/runtime_api boundaries, not just inside the engine.

### #2982 — The logic half is done (mode/thinking changes are locked while a turn is busy, with a status message and regression test). The headline ask — an obvious at-a-glance busy/idle visual indicator (color blocks / traffic light) — is still NOT implemented and the owner explicitly kept the issue open for a visual pass.

_files: crates/tui/src/tui/footer_ui.rs, crates/tui/src/tui/app.rs, crates/tui/src/palette.rs, crates/tui/src/tui/ui.rs · confidence: high_

Logic slice landed in commit 04aa64d56: App::reject_setting_change_while_busy posts "… is locked while a turn is running — press Esc to interrupt first" and cycle_mode/cycle_mode_reverse/cycle_effort honor it (crates/tui/src/tui/app.rs:2576-2624), covered by test mode_and_thinking_are_locked_while_a_turn_is_running (app.rs:5633). The visual indicator is the gap: footer_ui.rs has only a label/water-spout animation (footer_working_strip_active at crates/tui/src/tui/footer_ui.rs:238) and status_working uses DEEPSEEK_SKY (palette.rs:735) — there is no distinct high-contrast idle-vs-busy state block. Plan (4-6 lines): 1) Add a compact, always-present state chip in the footer/header that renders a clear busy/waiting-for-input/idle tri-state, driven by the same flags footer_working_strip_active already aggregates (is_loading, runtime_turn_status==in_progress, running agents, awaiting approval/user-input). 2) Use distinct palette colors (e.g. WHALE_ACCENT_PRIMARY/working vs a calm idle color) so it reads at a glance in narrow terminals. 3) Make the lock status message also clarify rejected-this-turn vs queued-for-next-turn. 4) Add a footer snapshot/rendering test for each tri-state. Needs design because it is a live-TUI visual decision (color/placement), not a mechanical edit; the regression-coverage criterion for the lock path is already satisfied.

### #3087 — No token-throughput (tok/s) display exists anywhere in the codebase. The raw token counters exist (last_completion_tokens, total_output_tokens, per-turn Usage) but there is no timing capture, no rolling-window smoothing, and no footer rendering of tok/s.

_files: crates/tui/src/tui/footer_ui.rs, crates/tui/src/tui/app.rs, crates/tui/src/core/engine/streaming.rs, crates/tui/src/core/engine.rs · confidence: high_

Greps for tok/s, tokens_per_sec, throughput return zero hits — feature is unbuilt. Available inputs: per-turn Usage with output_tokens (crates/tui/src/core/engine.rs:1935, 1671) and app-side session.last_completion_tokens / total_output_tokens (crates/tui/src/tui/app.rs:1207,1217); the footer render pipeline is render_footer / render_footer_from with composable *_spans helpers (crates/tui/src/tui/footer_ui.rs:24,504, e.g. footer_session_tokens_spans:722). Plan (4-6 lines): 1) Capture streaming output-token deltas with timestamps in the engine streaming path (crates/tui/src/core/engine/streaming.rs) or derive deltas from incremental Usage and emit them in the existing stream/usage events. 2) Add a small rolling-window smoother (struct holding recent (tokens, instant) samples) in app/session state, updated as output tokens arrive. 3) Add footer_throughput_spans in footer_ui.rs that renders "N tok/s" only while footer_working_strip_active is true and the provider exposes usable token deltas; show nothing/quiet state otherwise. 4) Gate behind a config flag for footer-noise-averse users. 5) Unit-test the smoothing math, the unavailable-data path, and a narrow-terminal truncation snapshot. Needs design because it touches the streaming/usage data flow and a deliberate fallback policy, not just a render tweak.

### #3190 — The core README rewrite is largely DONE — the manifesto opening (operating identity / ego / law) the issue complains about is gone, replaced by a factual 'What is CodeWhale', the deepseek-tui->CodeWhale history, a scannable provider list, doc links, and a concise community note. Remaining gaps: the required provider/model Mermaid graph is absent, and the localized READMEs are not synced.

_files: README.md, README.zh-CN.md, README.ja-JP.md, docs/PROVIDERS.md · confidence: medium_

README.md (201 lines) already satisfies most acceptance criteria: factual one-line description and 'What is CodeWhale' (README.md:13-26), the deepseek-tui->CodeWhale history and Chinese-community credit (README.md:21-24 and Community section ~line 118+), a scannable provider list (README.md:45-58), short install/first-run (README.md:62-110), and the doc-link set GUIDE/INSTALL/CONFIGURATION/PROVIDERS/SUBAGENTS/RUNTIME_API (README.md:140-148). Not yet met: (a) the explicit provider/model graph — grep shows no `mermaid`/`graph TD` block in README.md; (b) localized READMEs are out of sync (README.md=201 lines vs README.zh-CN.md=166, README.ja-JP.md=164, README.vi.md=207) so they need updating or explicit follow-up issues; (c) the issue also gates on docs/MODEL_LAB.md and CHANGELOG.md links and a provider-registry check (scripts/check-provider-registry.py). Plan (4-6 lines): 1) Add the Mermaid provider/model graph (or a compact table) matching shipped provider IDs in docs/PROVIDERS.md, marking experimental routes. 2) Add the missing MODEL_LAB.md and CHANGELOG.md links. 3) Reconcile the three localized READMEs or file tracking issues. 4) Run scripts/check-provider-registry.py (or manually reconcile) so the graph matches the registry. This is documentation-design work (accuracy + cross-file sync), not a one-liner.

### #1679 — Windows multi-agent SSE fanout: ~45s timeout + TUI corruption when 4-6 sub-agents run a review. This is the same blocking-fanout root cause as #3216/#2211, surfaced on Windows 11 / PowerShell where the long blocking wait is more visible. Reporter also notes /compact later fails because a sub-agent is present.

_files: crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/tools/subagent/mod.rs, crates/tui/src/tools/spec.rs, crates/tui/src/config.rs · confidence: high_

Subsumed by the #3216 worker-substrate redesign; not independently fixable here. Evidence it is still blocking: agent_open/agent_eval do NOT start detached (crates/tui/src/tools/spec.rs:683 default starts_detached_for returns false; only shell/verifier/tasks override it), so the parent turn holds open via should_hold_turn_for_subagents (crates/tui/src/core/engine/turn_loop.rs:2478-2480) while each child step waits up to the 120s per-step API timeout (crates/tui/src/config.rs:31 DEFAULT_SUBAGENT_API_TIMEOUT_SECS=120). The '45s' is the user's older 0.8.37 build. Build seams for the broader fix: turn_loop.rs:1090-1160 (the running_count hold loop) and the fleet detach cutover. Separately, the '/compact fails because a sub-agent is present' complaint (raised in comments) is a smaller guardrail worth a follow-up issue. Recommend: keep on milestone as a Windows-flavored duplicate of #3216, validate the freeze-fix + nonblocking work against a Windows fanout repro, and confirm UI redraw is clean under windows-terminal (the screenshot shows alt-screen corruption).

### #2211 — Sub-agent fanout plus hidden worktrees saturate the TUI during release work. Two slices already landed (workspace-discovery skip-list for .worktrees/.claude/worktrees/.deepseek/snapshots; running_count counts finished-but-unreconciled Running agents so a burst cannot refill the cap). Remaining open: nonblocking fanout/backpressure, saturated-cap queue behavior, and clearer active/completed/queued/stale sidebar states.

_files: crates/tui/src/tools/subagent/mod.rs, crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/tui/widgets/agent_card.rs, crates/tui/src/tools/subagent/tests.rs · confidence: high_

Partially done; the residual is the same nonblocking-worker redesign tracked in #3216 and is too large to land unattended. Already-landed evidence on this branch: running_count keeps a finished task handle counted while status is still Running (crates/tui/src/tools/subagent/mod.rs:1953 plus regression test crates/tui/src/tools/subagent/tests.rs:1900 test_running_count_counts_running_agents_until_status_reconciles); the cap check at mod.rs:2045 and 2269 uses it. The owner's own comments (2026-06-14) explicitly moved the open remainder into v0.8.61 and linked #3216 as the runtime blocker. Build seams for the remaining work: should_hold_turn_for_subagents (turn_loop.rs:2478) must stop holding solely on running_children>0; saturated-cap path at mod.rs:2045-2055 should queue rather than reject; sidebar state classification in crates/tui/src/tui/widgets/agent_card.rs. Recommend tracking the remainder under #3216 rather than re-implementing here.

### #3071 — Introduce a single versioned model-metadata registry instead of scattered hard-coded model facts (context window, max output, reasoning support, pricing, provider aliases, picker rows, router tiers). Acceptance criteria require a ModelMetadata API, existing helpers delegating to it, full coverage of current models, and a test that fails when a listed model lacks a registry entry.

_files: crates/tui/src/models.rs, crates/tui/src/pricing.rs, crates/tui/src/model_routing.rs, crates/tui/src/tui/model_picker.rs, crates/config/src/lib.rs · confidence: high_

Real refactor/epic, not a quick fix: the facts are still fully scattered and there is no registry abstraction (the only model_metadata grep hit, crates/tui/src/config.rs:11632, is just a test name). Build seams confirmed exactly as the issue lists them: context/output/reasoning match tables at crates/tui/src/models.rs:238-363; pricing at crates/tui/src/pricing.rs (613 lines); router candidates at crates/tui/src/model_routing.rs:53-76; picker rows/hints at crates/tui/src/tui/model_picker.rs:372-465; provider/model consts at crates/config/src/lib.rs:23-80. Plan: (1) add a checked-in RON/JSON snapshot plus a ModelMetadata accessor crate/module with provenance + last_verified fields; (2) reimplement context_window_for_model / max_output_tokens_for_model / model_supports_reasoning / pricing lookup as thin delegations; (3) generate picker rows and router candidates from registry role-tags; (4) add a coverage test asserting every picker/provider-default model id resolves to a registry entry or an explicit 'unverified' marker. Sequence behind #3066 (pricing) and #2984 (Codex metadata) so their values seed the registry. Too large to land unattended.

### #3090 — Define a README + website localization matrix, source text, and drift checks; specifically add Russian (ru) as a next-priority locale and replace the hard-coded en/zh website toggle with a multi-locale selector + Accept-Language detection. A docs/LOCALIZATION.md matrix already exists but covers TUI locales (v0.7.6 era), not the website, and does not list ru.

_files: web/lib/i18n/config.ts, web/components/locale-switcher.tsx, web/lib/i18n/get-dictionary.ts, docs/LOCALIZATION.md · confidence: high_

Partially addressed but the web-i18n acceptance criteria are real work. Already present: docs/LOCALIZATION.md is a 'Localization Matrix' with shipped/planned tiers and Arabic RTL deferral (LOCALIZATION.md:1, 49-63). README language links are already symmetric across all four files (verified README.md:6, README.zh-CN.md:5, README.ja-JP.md:5, README.vi.md:5). Still open: (1) website i18n is hard-coded to en/zh (web/lib/i18n/config.ts:1 locales=['en','zh']; only en.ts/zh.ts dictionaries exist); (2) web/components/locale-switcher.tsx:6-13 is a hard-coded two-locale toggle computing a single opposite; (3) web/lib/i18n/get-dictionary.ts:3-6 imports only en/zh; (4) no 'ru' anywhere and the existing matrix says nothing about the website surface. Plan: extend config.ts locales to a typed list, make the switcher map over locales with native labels, add a dictionary-presence drift test (npm --prefix web test), generalize Accept-Language detection beyond zh-vs-en, and add ru as next-priority in LOCALIZATION.md with a README/website split. Multi-file UX + i18n change that needs design; not a safe unattended quick-fix.

### #3192 — Submit CodeWhale to the agentclientprotocol/registry so editors like Zed can install/use it. CodeWhale already ships an ACP stdio adapter (codewhale serve --acp). The owner pulled this into v0.8.61 to weigh it with always-on agent / runtime-API / integration-surface work. It is primarily an external PR to another repo plus an ACP-completeness decision.

_files: crates/tui/src/acp_server.rs, crates/tui/src/main.rs, docs/RUNTIME_API.md · confidence: medium_

Treat as a small epic / decision rather than a quick code fix, because (a) the deliverable is an external registry PR to agentclientprotocol/registry (the reporter already opened registry issue #302) which cannot be done from this repo unattended, and (b) the current adapter is minimal: crates/tui/src/acp_server.rs (521 lines) handles initialize/session.new/session.prompt but session/cancel is a no-op stub (acp_server.rs:135 returns json!(null)) and there is no streaming/permission/fs capability surface. Before listing, decide whether the baseline adapter is registry-ready or whether cancel + richer session/update streaming should land first (build seam: AcpServer::dispatch at acp_server.rs:125-138 and write_session_update). Recommend: scope a short 'ACP registry readiness' issue covering the manifest entry, the external PR, and the cancel/streaming gaps; do not auto-land. (Docs already reference the adapter: docs/RUNTIME_API.md:5.)

### #3216 — Make multi-agent fanout durable and nonblocking before shipping swarm UX, and prove the TUI stays live under six-worker fanout (separate UI-liveness diagnosis track). Only the cancellation slice has landed: the turn loop now observes the cancel token between tool batches, turning a wedged ~24s burst into a prompt interrupt. The parent is still blocking-by-design during fanout.

_files: crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/tools/subagent/mod.rs, crates/tui/src/fleet/mod.rs, docs/V0_8_61_RELEASE_TRIAGE.md · confidence: high_

Release-blocker-scale design work; the landed fix is explicitly partial. Evidence: commit a6946812d added the between-batch cancel check (interrupted_tool_result at turn_loop.rs:2508) and its own message states it does NOT make the parent nonblocking. should_hold_turn_for_subagents still returns queued>0 || running_children>0 (turn_loop.rs:2478-2480) and the hold loop at turn_loop.rs:1090-1160 still waits while running_count()>0; agent_open/agent_eval are not detached (spec.rs:683) and turn_loop never calls fleet:: . docs/V0_8_61_RELEASE_TRIAGE.md:29-116,206-208 marks this 'In progress' with the freeze-fix as only the core chunk. 4-6 line plan: (1) change should_hold_turn_for_subagents so running children alone do not hold the turn, resuming only on queued completions or an explicit join; (2) add an explicit agent_eval(block:true)/fleet-wait finalize op; (3) detach high-fanout onto the durable fleet worker path (worker ids, leases, heartbeats, receipts) per docs/AGENT_RUNTIME.md cutover; (4) keep steering live via the rx_steer arm; (5) add the running-children-do-not-hold unit test, a two-worker steer-accept turn-loop test, and a six-fake-worker stress smoke; (6) add event-loop liveness instrumentation + a watchdog dump and audit for locks held across .await. Do not promote /swarm until done. Far beyond unattended quick-fix scope.

### #1737 — Failed shell command can leave TUI in indefinite in_progress with duplicate task_shell_wait rows and a still-running background job. Generic stuck-turn recovery and a job-cancel path already landed, but the specific dedup + failure-vs-live-child settling remains.

_files: crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/tools/shell.rs, crates/tui/src/tui/active_cell.rs, crates/tui/src/tui/ui.rs · confidence: high_

Partially mitigated, not closed. reconcile_turn_liveness (commit 06da97089) auto-heals the 'in_progress latched while is_loading cleared' orphan -> marks interrupted; this covers AC#3 for the generic stall. A cancel/cleanup path also exists: /jobs cancel and /jobs cancel-all (ui.rs:3310, 7200-7218) plus exec_shell_cancel tool (shell.rs:2741-2745) satisfy AC#4. STILL OPEN: (1) AC#2 dedup of repeated task_shell_wait live-tool rows -- no coalescing exists; the wait tool just returns 'Wait canceled; background shell task still running' (shell.rs:2915) and each poll re-renders a row. (2) The sidebar still presents in_progress when a foreground tool fails but a background job remains registered, because liveness is driven by ToolStatus::Running cells (active_cell.rs:289-314) without distinguishing 'failed command + orphaned background job' from 'genuinely working'. (3) remember_stale_job (shell.rs:1659) is #[allow(dead_code)] -- restart-stale jobs are not actually remembered. Build seams: ToolStatus settling on tool failure in turn_loop.rs (Failed paths at 223/436/2385); live-tool row aggregation feeding sidebar (active_cell.rs:289-332); list_jobs/stale surfacing (shell.rs:1636-1655, ui.rs:1006/7171). Needs a small design: a coalesced 'waiting on background job N (Mm, no output)' row plus a sidebar state that reads 'failed; background job running' instead of generic in_progress.

### #2475 — YOLO mode strands the run when a Burp MCP stdio proxy emits a startup/connection prompt instead of JSON-RPC. Only the diagnostic preview slice landed; the determinism fix (auto-resolve or degrade-to-error) is explicitly still open per the maintainer.

_files: crates/tui/src/mcp.rs, crates/tui/src/tools/registry.rs, crates/tui/src/core/engine.rs · confidence: high_

Diagnostic-only progress landed; root fix deferred by the owner in-thread. The harvested slice is present: invalid_json_preview (mcp.rs:199-212) and the recv() path now wrap non-JSON server output in 'Invalid MCP JSON-RPC message from server ...' with bounded, redacted, single-line preview (mcp.rs:1862-1876), covered by call_method_invalid_json_includes_server_output_preview (mcp.rs:4031-4045) and redaction tests (mcp.rs:4728+). This only improves VISIBILITY of the blocking prompt -- it does not make YOLO deterministic. The issue's actual ask (pre-resolve local stdio proxy startup prompts when safe, OR convert the prompt into a clear MCP server/tool error so turn dispatch does not hang) is unimplemented; the owner's last comment (2026-06-01) confirms it is intentionally left open. Implementation plan: (1) in the stdio transport handshake, bound the initialize/tools-list read with a timeout and on non-JSON-RPC bytes return a hard tool/connection error instead of blocking; (2) thread the YOLO/auto-approve posture (auto_approve, runtime_threads.rs:133) into MCP session setup so an interactive server prompt cannot strand a non-interactive run; (3) surface the redacted preview already produced as the error body. Build seams: recv()/handshake in crates/tui/src/mcp.rs (~1858-1876), MCP adapter registration in tools/registry.rs:900-907, MCP session construction in core/engine.rs. Multi-step and touches the connection state machine -> design, not a quick-fix.

### #3004 — Request to resolve api_key dynamically via a user script/helper (keepassxc/chezmoi/keyring), like Claude Code's apiKeyHelper, instead of plaintext in config.toml/.env. No subprocess key-resolution exists today.

_files: crates/config/src/lib.rs, crates/tui/src/hooks.rs · confidence: high_

Not implemented; confirmed enhancement. Grep finds no api_key_command/apiKeyHelper/credential_helper anywhere (only OS keyring storage via the Secrets facade, config/src/lib.rs:2289-2300, 3277). Today api_key is a plain Option<String> resolved from config-file > env > keyring (lib.rs:298, 609, 7214-7303). Implementation plan: (1) add an optional [providers.<p>] field like api_key_command (or top-level api_key_helper) to the provider/config structs (lib.rs:298/609); (2) in the secret-resolution chain (resolve_runtime_options_with_secrets, lib.rs:2293-2422) add a tier that, when api_key_command is set and no literal key is present, runs the command via std::process::Command, trims stdout, and uses it as the key; (3) cache within the process to avoid re-spawning per request, and never persist the resolved value; (4) document precedence (literal key > helper command > env > keyring). Build seams: provider structs and the resolution match in crates/config/src/lib.rs (~1446-1562 lists per-provider api_key reads), hooks.rs already runs subprocess env injection (hooks.rs:53, 821) and is a reference for safe Command spawning. Real design surface (precedence, caching, security/redaction, cross-platform shell) plus tests -> design, not unattended quick-fix. The owner intake note and a community comment both signal lower priority.

### #3072 — Bring the website to parity with existing Japanese and Vietnamese README locales: add ja/vi to web i18n config, dictionaries, a scalable locale switcher, and Accept-Language routing. Route structure is already locale-ready; the two-locale switcher must be redesigned.

_files: web/lib/i18n/config.ts, web/lib/i18n/get-dictionary.ts, web/components/locale-switcher.tsx, web/middleware.ts, web/lib/i18n/dictionaries/en.ts · confidence: high_

Not implemented; bounded but multi-file. Current state: config.ts exposes only ['en','zh']; dictionaries/ holds only en.ts and zh.ts; get-dictionary.ts hard-maps en/zh (lines 4-5); locale-switcher.tsx is a hard binary toggle (switchTo = current==='zh' ? 'en' : 'zh'); middleware.ts negotiates only over the config locales list. Pages live under web/app/[locale]/ (admin/contribute/docs/faq/feed/install present) so routes already scale. Implementation plan: (1) extend locales to add 'ja' and 'vi' in config.ts (defer exact ja vs ja-JP alias to #3090 if it lands first); (2) add dictionaries/ja.ts and dictionaries/vi.ts mirroring en.ts shape (nav/footer/localeSwitch) and register them in get-dictionary.ts; (3) replace the binary toggle in locale-switcher.tsx with a list/dropdown of all locales with LABELS for each; (4) middleware.ts Accept-Language already iterates the locales list, so 'ja'/'ja-JP'/'vi' resolve once added, but add explicit primary-tag matching for ja-JP/vi-VN; (5) translate the public pages' inline copy. Verification is defined in-issue (npm --prefix web run lint/test/build). This is real translation + a switcher redesign across 4-5 files -> design (epic-ish but well-scoped), not a 60-line quick-fix.

### #3091 — Audit all helper hints, footer/shortcut prompts, and inline affordance copy so every advertised command/keybinding is accurate and approachable, with friendly tips and visible fallbacks for hidden chords like Alt+V. Infrastructure (keybinding registry, tests, plain-v fallback) exists; the cross-surface audit is the work.

_files: crates/tui/src/tui/keybindings.rs, crates/tui/src/localization.rs, crates/tui/src/tui/footer_ui.rs, crates/tui/src/tui/history.rs · confidence: medium_

Largely a verification/polish epic, not done as a unit. Good news that narrows it: there IS a central keybinding registry with a test harness (keybindings.rs:308-373 assert non-empty chords, unique ranks, doc cross-ref), Alt+V is registered (keybindings.rs:213-217) and localized across all 6 languages (localization.rs CtxInspAltVHint at 1804/2441/2598/3222/3752 + ja/pt/es), and a VISIBLE fallback already exists -- plain 'v' on an empty composer routes to the same detail view (registry entry keybindings.rs:208-212; handler ui.rs:4519 Char('v')|Char('V')). So the motivating 'Alt+V is scary/hidden' concern is partly satisfied. Remaining work matches the ACs: (1) inventory helper copy across command palette/slash menu, footer hints (footer_ui.rs:338), empty states, collapsed tool rows ('Alt+V for details' history.rs:309/372, ui.rs:10052-10064), Work/WhaleFlow/Fleet panels, and docs; (2) verify each advertised chord actually fires on its surface; (3) add plain-language tip copy (e.g. 'Alt+V (or v) shows full details') and Option-key/Esc fallback wording; (4) extend snapshot/keybinding tests. Build seams: keybindings.rs (registry + tests), localization.rs (hint strings), footer_ui.rs, history.rs/ui.rs (collapsed-row affordances). Cross-cutting audit + many small copy/test edits across surfaces -> design (polish epic), too broad to land safely as one unattended quick-fix.

### #3194 — Duplicate of the helper-hint audit (this number maps to the same v0.8.61 'audit helper command hints / friendly tips for hidden shortcuts' polish work as 3091). Same disposition and seams.

_files: crates/tui/src/tui/keybindings.rs, crates/tui/src/localization.rs, crates/tui/src/tui/footer_ui.rs, crates/tui/src/tui/history.rs · confidence: medium_

NOTE: the fetched issue body for #3194 is the same 'v0.8.61: Audit helper command hints and add friendly tips for hidden shortcuts' content as #3091 (the gh view returned identical title/ACs). Treat as the same workstream. Same evidence: central keybinding registry + test harness (keybindings.rs:308-373), Alt+V registered (keybindings.rs:213-217) and localized (localization.rs CtxInspAltVHint), and the visible plain-'v' fallback already routes (keybindings.rs:208-212; ui.rs:4519). Remaining: cross-surface inventory of advertised commands/chords (slash menu, footer footer_ui.rs:338, collapsed tool rows history.rs:309/372, ui.rs:10052-10064, Fleet/WhaleFlow panels), verify each fires, add plain-language tip + Option/Esc fallback wording, extend snapshot/keybinding tests. Disposition design (polish epic), not a single safe quick-fix. If #3194 and #3091 are genuinely distinct in the tracker, dedupe at triage; the codebase work is one and the same.

### #1768 — Per-sub-agent model selection (Option A) is ALREADY shipped; only the global-default config (Option B) is genuinely missing — and wiring a new config field through to every spawn path is design-scale, not a safe unattended quick-fix.

_files: crates/tui/src/tools/subagent/mod.rs, crates/tui/src/model_routing.rs, crates/config/src/lib.rs, crates/tui/src/tools/subagent/tests.rs · confidence: high_

Already-done parts (cite when commenting): agent_open exposes an optional per-child `model` string (crates/tui/src/tools/subagent/mod.rs:3018-3021) and the parallel spawn tool exposes the same with explicit 'explicit wins over role/type defaults; omit to inherit' semantics (mod.rs:3289-3292). SpawnRequest/SubAgentSpawnOptions carry `model: Option<String>` (mod.rs:1015, 1068). Auto-routing for unconfigured children exists (model_routing.rs:42-94 RouterCandidates + auto_model_heuristic; subagent role_models HashMap at mod.rs:1212, with_role_models at mod.rs:1372). Availability/fallback error messaging exists (normalize_requested_subagent_model + annotate_child_model_error, tests.rs:2411-2436, 3642-3662). Backward-compat (omit -> inherit) is the current default. REMAINING (Option B global default): there is no `sub_agent_default_model`/`sub_agent_fallback_model` config field — grep of crates/config/src/lib.rs finds none. Implementation seam: add the field(s) to Config near the provider model constants (crates/config/src/lib.rs:22-80), thread the default into the spawn assignment resolution where `model: Option<String>` is consumed (crates/tui/src/tools/subagent/mod.rs:2037-2073 build path) so an unset per-call model falls back to config default then to auto/inherit. Honest recommendation: comment that the core feature request (per-task model) is already satisfied and either close as already-done or scope a small follow-up for the global default. Do NOT bundle as an unattended quick-fix because it touches config schema + spawn resolution + needs tests across role/explicit/default precedence.

### #2487 — This is the canonical multi-root-cause stalled-turn/dispatch-timeout thread. Several concrete causes are addressed on this branch (YOLO auto-approve direct path, cancel-between-batches, liveness watchdog recovery), but it is explicitly a multi-cause umbrella (Windows shell process-tree, context pressure, provider timeout, dispatch liveness) and should not be treated as fully closed.

_files: crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/tui/ui.rs, crates/tui/src/tools/subagent/mod.rs, docs/V0_8_61_RELEASE_TRIAGE.md · confidence: medium_

Already-landed slices (cite): (1) YOLO/auto-approve no longer round-trips approval through the UI — approval_required is forced false when registry.context().auto_approve is set (crates/tui/src/core/engine/turn_loop.rs:1536-1538), which is exactly the 'auto-approve liveness gap' aboimpinto reported. (2) Cancellation is observed between tool batches so a 6x~4s serial agent_open fan-out can be interrupted instead of reading as a hard freeze (turn_loop.rs:1688-1712, #3216/#2211). (3) A liveness watchdog recovers the UI out of a stuck busy state and surfaces the exact 'Turn stalled — no completion signal received' / 'Turn dispatch timed out' toasts instead of a permanent freeze (crates/tui/src/tui/ui.rs:4764-4847, reconcile_turn_liveness called at ui.rs:2719). Release triage doc confirms the freeze fix is partial — cancellation only, not full nonblocking fanout (docs/V0_8_61_RELEASE_TRIAGE.md sec 2, sec 4). REMAINING for a real fix/epic: the underlying serial agent_open holds the process-global tool_exec_lock across a 4s network model route (per triage doc seam refs turn_loop.rs serial batch + tool_execution.rs lock + subagent/mod.rs flash router 4s timeout); full nonblocking fanout is the durable-worker substrate work. Recommendation: keep open; comment that #3216/#2211 + YOLO auto-approve + watchdog narrow it, but retest is needed and remaining provider-timeout/context-pressure paths stay tracked. Not a quick-fix.

### #3073 — Confirmed open and accurately scoped: every hard-coded model surface the issue lists still exists, docs/MODEL_METADATA.md does not exist, and there are no drift-prevention tests. This is an audit + documentation + lint-test epic, not a small change.

_files: docs/MODEL_METADATA.md, crates/config/src/lib.rs, crates/tui/src/models.rs, crates/tui/src/tui/model_picker.rs, crates/tui/src/model_routing.rs · confidence: high_

Verification of current state (cite): provider defaults / model constants are still hard-coded (crates/config/src/lib.rs:22-80, including DEFAULT_OPENAI_CODEX_MODEL='gpt-5.5' at lib.rs:28); context-window lookup is a hand-maintained match (crates/tui/src/models.rs:238-300 context_window_for_model + known_context_window_for_model); model_picker has hard-coded rows/hints (crates/tui/src/tui/model_picker.rs:468-471); auto-router candidates are hard-coded (crates/tui/src/model_routing.rs:42-74 RouterCandidates::deepseek + provider_router_candidates); prompt model facts via apply_model_template (crates/tui/src/prompts.rs:~840). `ls docs/MODEL_METADATA.md` -> No such file. grep finds no `model_metadata` test. Implementation plan (4-6 lines): 1) Author docs/MODEL_METADATA.md enumerating each surface above with a migration decision (registry-now / keep-as-policy / temporary-with-expiry). 2) Decide the single metadata source (extend models.rs lookups or a new registry module consumed by picker/router/prompts/pricing). 3) Add drift tests asserting every picker-visible / provider-default model id has a context-window entry and pricing provenance (new tests under crates/tui/src/tui/model_picker.rs + crates/tui/src/models.rs). 4) Explicitly cover Codex/ChatGPT and OpenRouter-curated ids. Build seams: config/src/lib.rs:22-80, models.rs:238-325, model_picker.rs:468, model_routing.rs:42-74, prompts.rs:~840. Sizeable cross-cutting work — defer to a tracked task, not unattended.

### #3203 — The core asks already landed in v0.8.60 (Ctrl+S sends the queued/draft message; Ctrl+Enter/Cmd+Enter steers into a busy turn). The maintainer retargeted the REMAINDER to 0.8.61: Cmd+Enter reliability assurance, a busy/model-wait app-state regression, and clearer queued-steering status. That remainder is real but small/targeted work.

_files: crates/tui/src/tui/ui.rs, crates/tui/src/tui/ui/tests.rs, crates/tui/src/tui/composer_ui.rs, crates/tui/src/composer_stash.rs · confidence: medium_

Already-done (cite): Ctrl+S first attempts send_ctrl_s_queued_message_now() and only falls back to draft-stash when there is nothing to send (crates/tui/src/tui/ui.rs:4433-4452, helper at ui.rs:5378). Ctrl+Enter (which the composer maps Cmd+Enter onto via the CONTROL modifier, see composer_ui.rs:130) force-steers into the current turn when busy and otherwise submits (ui.rs:4136-4181), and submit_or_steer_message handles the Steer/Queue/Immediate disposition with scheduler-aware steering (ui.rs:7317-7357, pending_steers drain/merge at ui.rs:2129-2141, 7365). v0.8.60 commit 5ca618d70 made eval + queued steering nonblocking. REMAINING (4-6 line plan): 1) Audit every busy sub-state (model-waiting, child-fleet wait) to guarantee Cmd+Enter/Ctrl+Enter reaches the steer path and is never swallowed while workers are blocked — seam: decide_submit_disposition + steer_user_message around ui.rs:7317-7357 and the is_loading branch at ui.rs:4162-4179. 2) Add a PTY/app-state regression simulating a model-waiting turn that sends queued steering and asserts acceptance without waiting on children — extend tests near crates/tui/src/tui/ui/tests.rs:5732 (existing ctrl+s send tests). 3) Make the Work/Agents/status surface visibly distinguish queued user steering from pending tool/model work. Could be split: the regression + status label are quick, but the busy-state reliability audit needs design care, so classify as design overall.

### #3218 — Confirmed open and correct: /swarm is still a prompt-only fanout button that tells the model to open one in-process agent_open per subtask — exactly the unsafe pattern the issue wants gated. Gating it properly is tied to the durable-worker/goal-mode epics (#3215/#3216/#3217/#3205) and the still-incomplete nonblocking fanout, so it is design-scale.

_files: crates/tui/src/commands/groups/core/mod.rs, docs/FLEET.md, docs/MODES.md, docs/V0_8_61_RELEASE_TRIAGE.md · confidence: high_

Current state (cite): the swarm command builds a free-text instruction 'Run a multi-agent swarm... open one headless sub-agent per subtask with agent_open... poll each worker with nonblocking agent_eval' and dispatches it as an ordinary user message (crates/tui/src/commands/groups/core/mod.rs:379-402); it is registered and visible in slash completion via SWARM_INFO (mod.rs:127-132, registered mod.rs:39, dispatched mod.rs:295) with aliases fanout/qun — no feature gate, no Fleet/goal routing. docs/FLEET.md:45 and docs/MODES.md:15 still describe swarm-style fanout as available. Per docs/V0_8_61_RELEASE_TRIAGE.md sec 4 the intended contract is to gate /swarm until the durable substrate holds and the freeze fix is currently cancellation-only (partial). Plan (4-6 lines): 1) Decide contract: hide/experimental-gate /swarm OR reroute it through the durable Fleet/goal path instead of prompt-only agent_open. 2) Add a visibility gate (feature flag / readiness check on goal mode #3215, nonblocking parent #3216, worker profiles #3217, worker model routing #3205) at the command registration seam (mod.rs:39 + run_swarm mod.rs:240-241 + swarm() mod.rs:379). 3) Replace the prompt-only body with a Fleet task spec / goal-scoped fanout enqueue. 4) Add slash-completion/help tests for gated-on vs gated-off and a six-fake-worker smoke. 5) Update docs/FLEET.md, docs/MODES.md, and slash help to state stable/experimental/disabled. Blocked on the durable-worker epics — defer to a tracked design item, not unattended.

## defer

### #2629 — SiliconFlow / Tencent TokenHub 401 'invalid api key'. CodeWhale already sends a correct Authorization: Bearer <key> on the OpenAI-compatible path, so the issue's hypothesized root cause (missing/malformed Bearer) is not the bug. The remaining likely cause is provider-resolution/config and is unreproducible without the reporter's doctor output.

_files: crates/tui/src/client.rs, crates/config/src/lib.rs, crates/config/src/provider.rs · confidence: medium_

Defer as needs-info / not reproducible: the Bearer header the issue blames is already correct, so there is no safe unattended code change. Evidence the header is right: build_default_headers inserts AUTHORIZATION = 'Bearer {api_key}' for every non-Anthropic provider including generic openai (crates/tui/src/client.rs:737-744), with tests asserting 'Bearer sk-...' (client.rs:1936-1979). SiliconFlow has first-class providers with correct China endpoint (siliconflow / siliconflow-CN; DEFAULT_SILICONFLOW_CN_BASE_URL='https://api.siliconflow.cn/v1' at crates/config/src/lib.rs:93) and a generic 'openai' provider that passes base_url through (crates/config/src/provider.rs:146). As the owner already noted on the issue, the probable failure is the user setting [providers.openai].base_url but leaving the top-level provider unset so the default DeepSeek route resolves instead of openai. Next step is to obtain a redacted `codewhale doctor --json` (provider/base_url/model) per the owner's request and reproduce against a real gateway; only then decide if a generic-gateway fix is warranted. Too risky to guess a Tencent-specific change for the 0.8.61 corrective release.

### #2739 — Reporter (0.8.53, Windows) hits a task hang during long bug-fix work; Esc then 'continue' yields a connection-timeout loop, and re-entering via --continue loses the prior session history. The hard-freeze root cause is fixed on this branch, but the distinct continue-timeout and history-loss symptoms are not provably resolved and the issue is already cross-tagged to v0.8.64.

_files: crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/core/engine.rs, crates/tui/src/tui/persistence_actor.rs, crates/tui/src/tui/ui.rs · confidence: medium_

Defer the residual symptoms past the 0.8.61 gate (they need a Windows repro, not a blind code change). What IS already fixed on codex/v0.8.61: the sub-agent-fanout hard freeze and dead-cancel (#3216/#2211) — the serial tool loop now records interrupted tool_results and exits cleanly on cancel (crates/tui/src/core/engine/turn_loop.rs:1700-1721), Op::CancelRequest cancels then resets the token (crates/tui/src/core/engine.rs:1177-1180,585-597), and the 300s stream-chunk + subagent-heartbeat watchdogs exist (crates/tui/src/config.rs:42,48). What is NOT verified: (a) 'continue' looping on connection timeout after a cancel (likely a stale/un-reset stream client or a stuck request, distinct from cancel-token reset); (b) history loss on --continue — note in-turn checkpointing fires only while is_loading/is_compacting/is_purging (crates/tui/src/tui/ui.rs:2198-2203), so a turn cancelled and exited before a checkpoint coalesces may not persist the latest history. Action: ask reporter to retest on 0.8.61; if reproducible, instrument the post-cancel send path and ensure a SessionSnapshot is forced on cancel/exit. Build seams: crates/tui/src/tui/persistence_actor.rs:40-53,132-148 (PersistRequest variants/coalescing) and the cancel handling in engine.rs:1177.

### #3154 — Agent Fleet control-plane EPIC for always-running verifiable work. A local-first foundation (protocol, ledger, scheduler, local+SSH host adapters, exec-worker cutover, CLI passthrough, docs) is already landed; the owner retargeted the EPIC to v0.8.61 but explicitly scoped the large remaining pieces (long-lived manager/resume, event-driven planner wakeups, SSH remote proof, day-scale runbooks, goal-controller integration) as post-gate work.

_files: crates/tui/src/fleet/manager.rs, crates/tui/src/fleet/host.rs, crates/tui/src/fleet/ledger.rs, crates/protocol/src/fleet.rs, crates/cli/src/lib.rs · confidence: high_

Defer as an EPIC: too large and wrong blast radius for the 0.8.61 release gate, per the owner's own 2026-06-14 retarget comment. Substantial foundation already exists: fleet module with manager/scheduler/executor/ledger/host/alerts/worker_runtime (crates/tui/src/fleet/), protocol types (crates/protocol/src/fleet.rs), both LocalProcess and Ssh host adapters incl. SshFleetHostConfig and an ssh-command builder (crates/tui/src/fleet/host.rs:114-116,371-450 and the SshFleetHostAdapter), CLI surface fleet init/run/status/inspect/logs/artifacts/interrupt/restart/stop (crates/cli/src/lib.rs:203-204,586,2463-2477), exec-worker cutover (git commit 4f0d26109), and docs/FLEET.md. Remaining EPIC slices (long-lived resume from ledger, persisted/debounced/idempotent planner wakeups, barrier/heartbeat-driven continuation, day-scale runbook) should stay tracked but out of the release gate. Keep open as the umbrella; do not attempt in 0.8.61.

### #3084 — Introduce a typed ProviderAdapter contract + conformance tests to replace scattered match ApiProvider branches across config/routing/picker/doctor/clients. Large cleanup epic explicitly coupled to the #3071/#3072/#3073/#3075/#3083 model-lab series.

_files: crates/tui/src/config.rs, crates/tui/src/model_routing.rs, crates/tui/src/tui/provider_picker.rs · confidence: high_

Too large and wrong blast radius for 0.8.61: it rewrites provider capability/auth/dialect/model-id/pricing resolution into one adapter layer and adds conformance tests for every shipped provider, touching config.rs::provider_capability (crates/tui/src/config.rs:415), the normalize/wire/completion-name match tables, model_routing.rs::provider_router_candidates, tui/provider_picker.rs hints, main.rs::provider_capability_report, and the client/* dialect modules. The issue itself frames it as the provider side of the #3071/#3072 epic. Defer to the model-lab track.

### #3166 — Agent Fleet end-to-end dogfood smoke: larger local smoke (10+ tasks, 3+ workers, failure injection, stale/retry), Runtime API parity, optional SSH worker smoke, endurance/soak (24h/72h), continuation wakeups, log-retention proof, and /goal-controlled long-run. Owner already retargeted and enumerated the remaining scope.

_files: crates/tui/src/fleet/manager.rs, crates/tui/src/fleet/scheduler.rs, docs/FLEET.md · confidence: high_

Multi-part epic, not landable unattended in 0.8.61. A minimal smoke already exists (crates/tui/src/fleet/manager.rs:1726 fleet_dogfood_smoke_run_two_local_workers_two_tasks, plus protocol/src/fleet.rs:870 fixture and docs/FLEET.md SSH worker docs), and the owner's 2026-06-14 comment explicitly says v0.8.60 shipped fake-subprocess dogfood + real exec worker cutover but that this issue's larger smoke, stale/retry proof, Runtime API parity, SSH smoke, endurance/soak, continuation wakeups, log-retention, and /goal-controlled run 'remain open follow-up work.' Build seams if pursued later: scheduler retry/stale policy in crates/tui/src/fleet/scheduler.rs, manager drive loop at manager.rs:324-347/699-730, ledger events in fleet/ledger.rs, SSH transport in fleet/host.rs + executor.rs:44-55. Defer as an epic.

### #1067 — Prebuilt Linux binaries require GLIBC_2.38/2.39 but server has glibc 2.35. Native-binary glibc floor, not closeable on this branch.

_files: .github/workflows/release.yml, npm/codewhale/scripts/preflight-glibc.js, npm/codewhale/scripts/install.js · confidence: high_

Release-pipeline change with broad CI blast radius, not a 0.8.61 quick-fix. .github/workflows/release.yml:113-114,138-139 builds x86_64/aarch64 Linux on `ubuntu-latest` (no glibc-floor container, no musl/static target), so the published binary's glibc floor tracks the hosted runner. The npm wrapper added a preflight guard (npm/codewhale/scripts/preflight-glibc.js: detects host glibc, refuses + routes to cargo/source) and install.js calls it at install.js:1042,1077 — but that only blocks/warns, it does NOT make the binary compatible with glibc 2.35. A real fix needs one of: a lower-glibc build container, a musl/static x86_64 asset, or cross/zig pinning — each a release.yml matrix change that must be dry-run against the release pipeline. Issue already carries both v0.8.61 and v0.8.69 labels; the owner's own comments say the same. Defer to a dedicated release-channel epic.

### #2924 — "Can't update via npm" — empty template body, no repro/logs/version from reporter; npm install/update path is robust and shows no diagnosable defect.

_files: npm/codewhale/scripts/install.js, npm/codewhale/scripts/run.js, crates/cli/src/update.rs · confidence: medium_

Needs-info, not actionable for 0.8.61. The issue body is the unmodified bug template (no Steps/Expected/Actual/logs/version filled in) and the only comment is the automated intake note; @tiangangQiu never supplied a failure mode. The npm update path is sound: `npm install -g codewhale@latest` bumps the package, and npm/codewhale/scripts/install.js postinstall (:1163) -> `ensureBinary` (:1055) re-downloads when the `.version` marker differs from pkg.codewhaleBinaryVersion (package.json pins 0.8.61), with 5x retry/backoff (:793), proxy/CONNECT support (:374), checksum verify (:989), and glibc preflight (:1077). run.js resolves the binary lazily on first run. crates/cli/src/update.rs:33,205-235 also handles the most likely real-world cause (a legacy `deepseek-tui` binary) with an explicit npm reinstall message. Without a concrete repro there is no code change to make safely. Defer pending reporter details; do not block the release.

### #3086 — Unified context-budget service. Explicitly an orchestration-layer epic with 8+ consumers that depends on sibling registry issues (#3070/#3071/#3072); too large and wrong blast radius for 0.8.61. Scattered budget logic exists today but no unified service.

_files: crates/tui/src/models.rs, crates/tui/src/core/engine/context.rs, crates/tui/src/config.rs · confidence: high_

DEFER: building a single ContextBudgetService that compaction, prompt facts, doctor --json, UI pressure, request-cap builders, tool-result truncation, and sub-agent budgeting all consume is a cross-cutting epic gated on #3070/#3071/#3072 (model metadata registry + provider catalog), far beyond a point release. Today the facts are genuinely scattered exactly as the issue states: crates/tui/src/models.rs:16,238,325,501 (DEFAULT_COMPACTION_TOKEN_THRESHOLD, context_window_for_model, max_output_tokens_for_model, compaction threshold), config.rs provider_capability (recomputes window/max-output per provider+model), and crates/tui/src/core/engine/context.rs:582-601 (context_input_budget_for_provider/_for_window — input-side budget only, used by preflight/emergency-recovery, with conservative unknown-model fallback and an openai_codex effective-window path). No ContextBudgetService type exists (grep across crates returns only display helpers like footer_ui.rs:988 format_context_budget). Leave for v0.9 model-lab work.

### #2058 — Full Codex goal-system port (9 phases, persistent continuation loop, SQLite goal store, tool surface, scheduler). Owner already re-scoped it: the actionable 0.8.61 slice moved to #3215, and the remainder is a v0.9.0 WhaleFlow LoopUntil consumer.

_files: crates/tui/src/tools/goal.rs, crates/tui/src/core/engine/turn_loop.rs, crates/state/src/lib.rs · confidence: high_

Wrong blast radius for 0.8.61 — owner comments (2026-06-10 and 2026-06-14) re-scope this to v0.9.0 as a WhaleFlow LoopUntil workflow and move the now-landing durable-goal slice into #3215; it is not even labeled v0.8.61.

### #3215 — Make goal mode a durable cross-turn work loop (persisted objective, continuation scheduler, steering contract, accounting, unified state bridge). Today there are three disconnected goal models and the continuation is only a bounded within-turn prompt injection. This is a multi-system epic, explicitly the 0.8.61 hub for #891/#1976/#2058.

_files: crates/tui/src/core/engine/turn_loop.rs, crates/tui/src/tools/goal.rs, crates/tui/src/commands/groups/project/goal.rs, crates/tui/src/tui/ui.rs, crates/state/src/lib.rs · confidence: high_

Too large / wrong blast radius to land unattended: the issue itself is a 9-step plan spanning crates/tui/src/commands/groups/project/goal.rs (427 lines, app.hunt-backed /goal), crates/tui/src/tools/goal.rs (571 lines, model tools + SharedGoalState), the bounded within-turn continuation at crates/tui/src/core/engine/turn_loop.rs:2390-2434 (capped, ends turn 'to avoid a runaway loop' at :2417), the app.hunt projection (crates/tui/src/tui/ui.rs:892-925, sidebar.rs:271), and the already-existing-but-unwired persisted ThreadGoal in protocol/state/app-server/core. Unifying three goal models into one durable bridge plus a real continuation scheduler is epic-scale; it needs its own design (the triage doc at docs/V0_8_61_RELEASE_TRIAGE.md:141-165 already treats it as the goal-mode hub).

### #2984 — OpenAI Codex provider: verify a live ChatGPT-OAuth round-trip and promote it out of preview. The provider is implemented (config, oauth, model_inventory, client) and still documented as Experimental in docs/PROVIDERS.md:146. The checklist requires a live OAuth account round-trip, gpt-5.5 default verification on a real account, and a docs promotion decision.

_files: docs/PROVIDERS.md, crates/config/src/lib.rs, crates/tui/src/oauth.rs, crates/tui/src/model_inventory.rs · confidence: high_

Defer: the core acceptance work (live ChatGPT-OAuth round-trip, real-account model listing, usage telemetry alignment tracked separately in #2955) requires a real authenticated OpenAI/Codex account and live network calls, which cannot be done safely or unattended in a release-prep pass and has wrong blast radius for 0.8.61. Implementation exists (crates/config/src/lib.rs DEFAULT_OPENAI_CODEX_MODEL=gpt-5.5 at line ~29, crates/tui/src/oauth.rs, crates/tui/src/model_inventory.rs, context budgeting in crates/tui/src/models.rs:252-254 at 400K). The remaining doc flip from 'Experimental' is a one-line PROVIDERS.md:146 edit but must NOT precede the live verification, so this stays a maintainer-gated task.

### #3217 — Give fleet/sub-agent workers explicit role-based effective runtime profiles (model route, permission profile, shell policy none/read-only/background/write, tool profile, depth, budget) resolved by one shared resolver and exposed in inspection. Explicitly a multi-PR epic, gated on #3211/#3212/#3205.

_files: crates/tui/src/fleet/worker_runtime.rs, crates/tui/src/tools/subagent/mod.rs, crates/tui/src/tools/registry.rs, crates/tui/src/core/engine.rs · confidence: high_

Too large for 0.8.61 and explicitly scoped as a multi-PR effort. docs/V0_8_61_RELEASE_TRIAGE.md calls out '#3211/#3217 permission profiles are multi-PR efforts' and that '#1806 (sub-agent timeout) rolled into #3216/#3217'. Current code is far from the target: AgentWorkerToolProfile is only a two-variant enum (Inherited | Explicit) in subagent/mod.rs; there is no EffectiveWorkerRuntimeProfile (grep finds none); shell access is still a single boolean with_agent_tools(allow_shell) (registry.rs:919-946, tool_setup.rs:58) -- the issue's step 3 explicitly wants this replaced by none/read-only/background/write permission profiles; filter_tool_profile cannot resolve Inherited at spec time and defers it to spawn ('Inherited profiles can't be filtered at spec time', worker_runtime.rs:255-260), exactly the spawn-time surprise step 8 wants removed; fleet and agent_open use different paths (worker_runtime.rs fleet_tool_profile vs registry with_agent_tools) rather than one shared resolver (step 2). Doing this properly requires: a new resolved struct, a shared resolver both paths call, parent>=child capability narrowing proofs, role defaults, model routing via #3205, and inspection surfacing -- plus the dependent issues #3211/#3212/#3205. Wrong blast radius for a point release; defer to its epic.

### #3092 — Russian localization is large, multi-surface (README + Next.js route + dictionary + locale switcher + Cyrillic QA) and the issue itself blocks on #3087 (README rewrite) and #3090 (multi-locale selector); wrong blast radius for an engineering-focused 0.8.61 and not currently started.

_files: README.md, web/lib/i18n/config.ts, web/components/locale-switcher.tsx, web/lib/i18n/dictionaries/en.ts · confidence: high_

Confirmed nothing exists: README.md language header lists only zh/ja/vi (README.md:5), no README.ru*.md on disk; web/lib/i18n/config.ts exposes only ["en","zh"]; web/lib/i18n/dictionaries/ has only en.ts and zh.ts; web/components/locale-switcher.tsx is a hardcoded binary toggle (LABELS = { en:'EN', zh:'中文' }; switchTo = current==='zh'?'en':'zh', lines 6-13) that cannot represent a third locale; docs/LOCALIZATION.md has no Russian entry. Per the issue's own acceptance criteria it must build on post-#3087 README source and needs the #3090 N-locale selector before Russian can ship cleanly, plus Cyrillic overflow QA across desktop/mobile. Defer: blocked on #3087/#3090 and out of scope for a freeze/runtime-focused 0.8.61.

